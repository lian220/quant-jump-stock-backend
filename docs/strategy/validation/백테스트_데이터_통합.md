# 백테스트 데이터 통합

**문서 타입**: 📝 설계 계획 + 📦 구현 완료 (통합 문서)
**작성일**: 2026-02-13
**출처**:
- 설계: `plans/백테스트_데이터_통합_계획.md`
- 구현: `architecture/백테스트_데이터_통합_전략.md` (SCRUM-324)

> **핵심 전략**: MongoDB 3개 컬렉션을 통합하여 OHLCV + 감성 분석 + 기술적 분석 데이터를 백테스트에 활용

---

## 📋 목차

**Part 1: 설계 계획 (Design Plan)**
1. [개요](#1-개요)
2. [현재 아키텍처 분석](#2-현재-아키텍처-분석)
3. [제안된 통합 전략](#3-제안된-통합-전략)
4. [구현 단계](#4-구현-단계)
5. [검증 전략](#5-검증-전략)

**Part 2: 구현 상세 (Implementation)**
6. [MongoDB 컬렉션 구조](#6-mongodb-컬렉션-구조)
7. [데이터 통합 구현](#7-데이터-통합-구현)
8. [사용 예시](#8-사용-예시)
9. [성능 및 확장성](#9-성능-및-확장성)
10. [향후 계획](#10-향후-계획)

---

# Part 1: 설계 계획 (Design Plan)

## 1. 개요

### 1.1 배경

본 문서는 백테스트 엔진(Backtest Engine)이 단순히 가격 데이터(OHLCV) 뿐만 아니라, **뉴스 감성 분석**(Sentiment Analysis) 및 **기술적 분석 추천**(Technical Recommendations) 데이터를 활용할 수 있도록 지원하기 위한 기술적 전략을 기술합니다.

또한, **PRD v2.0**에서 요구하는 '포트폴리오형 전략(워런 버핏 등)'을 지원하기 위한 전략 모델 확장을 포함합니다.

### 1.2 목표

MongoDB 3개 컬렉션을 통합하여 다차원 데이터 백테스트를 지원합니다:

1. ✅ **daily_stock_data**: OHLCV 기본 데이터
2. ✅ **sentiment_analysis**: 감성 분석 점수
3. ✅ **stock_recommendations**: 기술적 추천 지표

### 1.3 핵심 가치

- **전략 다양성**: SCREENING(종목 선별) + PORTFOLIO(포트폴리오 최적화) 이원화 지원
- **데이터 유연성**: Left Join 방식으로 선택적 데이터 로드
- **실전 활용**: 감성 + 기술적 지표를 활용한 하이브리드 전략 구현

---

## 2. 현재 아키텍처 분석

### 2.1 데이터 소스 (MongoDB 컬렉션)

현재 데이터는 서로 다른 목적과 스키마를 가진 3개의 컬렉션으로 분리되어 저장됩니다.

#### 1. `daily_stock_data` (시장 데이터)

**목적**: 일별 OHLCV (시가, 고가, 저가, 종가, 거래량) 데이터 및 주요 시장 지표 저장.

**스키마**:
```json
{
  "date": "YYYY-MM-DD",
  "stocks": {
    "AAPL": { "open": 150.0, "close": 155.0, ... },
    "TSLA": { ... }
  },
  "fred_indicators": { ... },
  "yfinance_indicators": { ... }
}
```

**현재 사용**: `MongoDataLoader`가 이 컬렉션만 읽어서 백테스트 엔진용 `pd.DataFrame`을 생성합니다.

#### 2. `sentiment_analysis` (대안 데이터)

**목적**: 뉴스/기사 기반의 종목별 AI 감성 분석 점수 저장.

**스키마**:
```json
{
  "date": "YYYY-MM-DD",
  "ticker": "AAPL",
  "article_count": 10,
  "average_sentiment_score": 0.75  // 범위: -1.0 ~ 1.0
}
```

**현재 사용**: 독립적인 분석 결과로만 존재하며, **백테스트에서는 사용되지 않음**.

#### 3. `stock_recommendations` (기술적 분석 결과)

**목적**: 매일 밤 실행되는 기술적 분석 배치 작업의 결과 (골든 크로스, RSI, MACD 등) 저장.

**스키마**:
```json
{
  "date": "YYYY-MM-DD",
  "ticker": "AAPL",
  "is_recommended": true,
  "technical_indicators": { "rsi": 45.5, "golden_cross": true, ... }
}
```

**현재 사용**: 독립적인 추천 결과로만 존재하며, **백테스트에서는 사용되지 않음**.

### 2.2 문제점 (The Gap)

1. **데이터 미사용**: `BacktestEngine`이 OHLCV 이외의 감성/추천 데이터를 로드하지 않아, 복합 전략 백테스트가 불가능합니다.
2. **전략 모델 한계**: 현재 전략 모델은 기술적 지표 필터링(`SCREENING`)만 가정하고 있으며, PRD가 요구하는 **고정 포트폴리오(`PORTFOLIO`)** 전략을 지원하지 못합니다.

---

## 3. 제안된 통합 전략

### 3.1 데이터 로더 확장 (`MongoDataLoader`)

`MongoDataLoader.load()`를 수정하여 보조 데이터를 메인 가격 데이터에 "Left Join" 방식으로 병합합니다.

**새로운 DataFrame 구조:**

종목(예: AAPL)별 `pd.DataFrame`에 새로운 컬럼이 추가됩니다:
- `open`, `high`, `low`, `close`, `volume` (기존)
- **`sentiment_score`** (신규, `sentiment_analysis`에서 가져옴, 기본값: 0.0)
- **`sentiment_count`** (신규, `sentiment_analysis`에서 가져옴, 기본값: 0)
- **`is_recommended`** (신규, `stock_recommendations`에서 가져옴, 기본값: False)
- **`rec_rsi`** (신규, `stock_recommendations.technical_indicators`에서 가져옴, 필요 시)

### 3.2 도메인 모델 업데이트 (`models.py`)

#### 1. 지표 확장
`IndicatorType` 열거형(Enum)을 확장하여 새로운 데이터 필드를 지원합니다.

```python
class IndicatorType(str, Enum):
    # ... 기존 타입들 ...
    SENTIMENT_SCORE = "sentiment_score"  # 신규
    IS_RECOMMENDED = "is_recommended"    # 신규
```

#### 2. 전략 구조 확장
`StrategyDefinition`에 종목 선정 방식을 명시하는 필드를 추가합니다.

```python
class StockSelectionType(str, Enum):
    SCREENING = "screening"   # 조건으로 종목 찾기 (기존 방식)
    PORTFOLIO = "portfolio"   # 고정 종목 리스트 (신규 지원)

class StrategyDefinition(BaseModel):
    # ... 기존 필드 ...
    stock_selection_type: StockSelectionType = Field(default=StockSelectionType.SCREENING)
    investment_philosophy: Optional[str] = Field(description="투자 철학 (AI 참고용)")
```

### 3.3 전략 인터프리터 로직 (`interpreter.py`)

- `_calculate_all_indicators`: 새로운 지표 타입(`SENTIMENT_SCORE`, `IS_RECOMMENDED`) 처리 로직 추가.
- `_filter_stocks` (신설/수정): `stock_selection_type`이 `PORTFOLIO`인 경우, 조건 필터링을 건너뛰고 지정된 종목 리스트를 그대로 사용하도록 분기 처리.

---

## 4. 구현 단계

### 4.1 Phase 1: `Strategy Models` 업데이트

`src/domain/strategy/models.py` 수정:
1. `IndicatorType`에 `SENTIMENT_SCORE`, `IS_RECOMMENDED` 추가.
2. `StockSelectionType` Enum 추가 및 `StrategyDefinition` 필드 확장.

### 4.2 Phase 2: `MongoDataLoader` 수정

`src/application/backtest/data_loader_mongo.py` 수정:
1. `load()` 내에서 `daily_stock_data` 로드 후, `sentiment_analysis` 및 `stock_recommendations` 컬렉션 추가 조회.
2. `ticker`와 `date` 기준으로 데이터를 병합 (`pd.merge`).
3. 결측치(NaN)에 대한 기본값 처리 (`0.0`, `False`).

### 4.3 Phase 3: `Strategy Interpreter` 업데이트

`src/domain/strategy/interpreter.py` 수정:
1. 새로운 데이터를 `IndicatorType` 매핑으로 연결.
2. `stock_selection_type`에 따른 실행 로직 분기 (유니버스 필터링 vs 고정 포트폴리오).

### 4.4 Phase 4: 검증 (테스트 전략)

새로운 기능을 검증하기 위한 통합 테스트 전략 생성 (아래 5. 검증 전략 참조).

---

## 5. 검증 전략

### 5.1 Case 1: 복합 필터링 (Screening)

```json
{
  "name": "감성 & 기술적 분석 결합 전략",
  "stock_selection_type": "screening",
  "rules": [
    {
      "signal_type": "buy",
      "conditions": [
        { "indicator": "sentiment_score", "operator": "gt", "value": 0.5 },
        { "indicator": "is_recommended", "operator": "eq", "value": 1.0 }
      ]
    }
  ]
}
```

### 5.2 Case 2: 고정 포트폴리오 (Portfolio)

```json
{
  "name": "워런 버핏 추종 전략",
  "stock_selection_type": "portfolio",
  "description": "애플, 뱅크오브아메리카 등 고정 종목 매수",
  "rules": [
    {
      "signal_type": "buy",
      "conditions": [ ...매수 타이밍 로직... ]
    }
  ]
}
```

### 5.3 예상되는 데이터베이스 스키마 변경

- **MongoDB**: 스키마 변경 없음. (애플리케이션 레벨 조인)
- **PostgreSQL**: `strategies` 테이블에 `stock_selection_type`, `investment_philosophy` 컬럼 추가 필요 (PRD v2.0 요구사항).

---

# Part 2: 구현 상세 (Implementation)

## 6. MongoDB 컬렉션 구조

### 6.1 daily_stock_data (기본 OHLCV)

```json
{
  "date": "2025-01-01",
  "stocks": {
    "AAPL": {
      "open": 150.0,
      "high": 155.0,
      "low": 148.0,
      "close": 153.0,
      "volume": 1000000
    },
    "NVDA": {
      "open": 500.0,
      "high": 510.0,
      "low": 495.0,
      "close": 505.0,
      "volume": 2000000
    }
  },
  "yfinance_indicators": {
    "S&P 500 ETF": 597.20,
    "NASDAQ 100 ETF": 512.34
  },
  "fred_indicators": {
    "CPIAUCSL": 315.456,
    "UNRATE": 3.7
  }
}
```

**특징**:
- 일별 문서 구조 (date가 키)
- stocks 안에 모든 종목 데이터 포함
- **레거시 포맷 지원**: close_price만 있는 경우 OHLCV 추정

### 6.2 sentiment_analysis (감성 분석)

```json
{
  "ticker": "AAPL",
  "date": "2025-01-01",
  "average_sentiment_score": 0.75,
  "article_count": 10,
  "sources": ["news", "twitter", "reddit"]
}
```

**특징**:
- 종목별 문서 구조 (ticker + date가 복합 키)
- 감성 점수: -1.0 (부정) ~ 1.0 (긍정)
- 기사 수: 신뢰도 지표로 활용

### 6.3 stock_recommendations (기술적 추천)

```json
{
  "ticker": "AAPL",
  "date": "2025-01-01",
  "is_recommended": true,
  "rsi": 45.5,
  "recommendation_score": 0.8,
  "indicators": {
    "macd": 1.23,
    "signal": 0.98
  }
}
```

**특징**:
- 종목별 문서 구조 (ticker + date가 복합 키)
- is_recommended: 매수 추천 여부 (boolean)
- rsi: 과매수/과매도 판단
- recommendation_score: 종합 추천 점수 (0~1)

---

## 7. 데이터 통합 구현

### 7.1 시스템 구조

```
┌──────────────────────────────────────────────────────────────────┐
│                        BacktestEngine                             │
│  (application/backtest/backtest_engine.py)                       │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ load()
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│                     MongoDataLoader                               │
│  (application/backtest/data_loader_mongo.py)                     │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  load(symbols, start_date, end_date,                       │ │
│  │       include_sentiment=True,                              │ │
│  │       include_recommendations=True)                        │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────┬──────────────┬──────────────┬────────────────────────────────┘
      │              │              │
      ▼              ▼              ▼
┌──────────┐  ┌──────────────┐  ┌─────────────────────┐
│ MongoDB  │  │   MongoDB    │  │     MongoDB         │
│ daily_   │  │ sentiment_   │  │ stock_              │
│ stock_   │  │ analysis     │  │ recommendations     │
│ data     │  │              │  │                     │
│          │  │              │  │                     │
│ - OHLCV  │  │ - score      │  │ - is_recommended    │
│ - date   │  │ - count      │  │ - rec_rsi           │
│ - ticker │  │ - date       │  │ - rec_score         │
│          │  │ - ticker     │  │ - date              │
│          │  │              │  │ - ticker            │
└──────────┘  └──────────────┘  └─────────────────────┘
```

### 7.2 Left Join 방식

**기본 원칙**:
- **기준**: daily_stock_data (OHLCV)
- **선택적 조인**: sentiment_analysis, stock_recommendations
- **결측값 처리**: 데이터가 없으면 기본값 사용 (0.0, False 등)

### 7.3 구현 로직

```python
# 1. OHLCV 기본 데이터 로드
loader = MongoDataLoader()
data = loader.load(
    symbols=["AAPL", "NVDA"],
    start_date=date(2025, 1, 1),
    end_date=date(2025, 1, 31),
    include_sentiment=True,           # 감성 분석 포함
    include_recommendations=True      # 기술적 추천 포함
)

# 2. 결과 DataFrame 구조
"""
date                open    high    low     close   volume  sentiment_score  sentiment_count  is_recommended  rec_rsi  rec_score
2025-01-01 00:00:00 150.0   155.0   148.0   153.0   1000000  0.75            10               True            45.5     0.8
2025-01-02 00:00:00 153.0   158.0   152.0   157.0   1200000  0.80            12               True            48.2     0.85
2025-01-03 00:00:00 157.0   160.0   155.0   158.0   900000   0.0             0                False           0.0      0.0
"""
```

### 7.4 Left Join 알고리즘

```python
# MongoDataLoader.load() 내부 로직

# Step 1: OHLCV 데이터 로드
documents = coll.find({"date": {"$gte": start_date, "$lte": end_date}})

# Step 2: 감성 분석 데이터 로드 (옵션)
if include_sentiment:
    sentiment_data = {
        (doc["ticker"], doc["date"]): doc
        for doc in sentiment_coll.find(...)
    }

# Step 3: 추천 데이터 로드 (옵션)
if include_recommendations:
    recommendation_data = {
        (doc["ticker"], doc["date"]): doc
        for doc in rec_coll.find(...)
    }

# Step 4: Left Join 수행
for symbol in symbols:
    for doc in documents:
        date_str = doc["date"]

        # OHLCV 기본 레코드 생성
        record = {
            "date": pd.Timestamp(date_str),
            "open": ...,
            "close": ...,
            # ...
        }

        # Left Join: 감성 분석
        if include_sentiment:
            sent_key = (symbol, date_str)
            sent_doc = sentiment_data.get(sent_key)
            if sent_doc:
                record["sentiment_score"] = sent_doc["average_sentiment_score"]
                record["sentiment_count"] = sent_doc["article_count"]
            else:
                record["sentiment_score"] = 0.0
                record["sentiment_count"] = 0

        # Left Join: 추천 데이터
        if include_recommendations:
            rec_key = (symbol, date_str)
            rec_doc = recommendation_data.get(rec_key)
            if rec_doc:
                record["is_recommended"] = rec_doc["is_recommended"]
                record["rec_rsi"] = rec_doc["rsi"]
                record["rec_score"] = rec_doc["recommendation_score"]
            else:
                record["is_recommended"] = False
                record["rec_rsi"] = 0.0
                record["rec_score"] = 0.0

        records.append(record)
```

### 7.5 레거시 데이터 처리

**문제**: 초기 데이터는 `close_price`만 존재 (OHLCV 중 O/H/L/V 없음)

**해결책**: 추정 배율 사용

```python
# 레거시 포맷 감지
if "open" not in stock_data:
    # close_price로 OHLCV 추정
    close = stock_data["close_price"]
    record.update({
        "open": close * 0.998,   # -0.2%
        "high": close * 1.005,   # +0.5%
        "low": close * 0.995,    # -0.5%
        "close": close,
        "volume": 1_000_000      # 기본값
    })
```

---

## 8. 사용 예시

### 8.1 감성 기반 SCREENING 전략

```python
# scripts/backtest_sentiment_strategy.py

from application.backtest.data_loader_mongo import MongoDataLoader

# 데이터 로드 (감성 분석 포함)
loader = MongoDataLoader()
data = loader.load(
    symbols=["AAPL", "NVDA", "TSLA"],
    start_date=date(2025, 1, 1),
    end_date=date(2025, 1, 31),
    include_sentiment=True
)

# SCREENING 조건: 감성 점수 > 0.2 AND 기사 수 >= 10
for symbol, df in data.items():
    candidates = df[
        (df["sentiment_score"] > 0.2) &
        (df["sentiment_count"] >= 10)
    ]
    print(f"{symbol}: {len(candidates)} days qualified")
```

**출력 예시**:
```
AAPL: 15 days qualified
NVDA: 20 days qualified
TSLA: 8 days qualified
```

### 8.2 하이브리드 전략 (감성 + 기술적 지표)

```python
# 감성 + 추천 모두 활용
data = loader.load(
    symbols=["AAPL", "NVDA"],
    start_date=date(2025, 1, 1),
    end_date=date(2025, 1, 31),
    include_sentiment=True,
    include_recommendations=True
)

# SCREENING 조건: 감성 긍정 + 기술적 매수 추천
for symbol, df in data.items():
    candidates = df[
        (df["sentiment_score"] > 0.3) &
        (df["is_recommended"] == True) &
        (df["rec_rsi"] < 70)  # 과매수 제외
    ]
    print(f"{symbol}: {len(candidates)} days qualified (hybrid)")
```

### 8.3 백테스트 실전 예시

```python
# 전략 설정
backtest = SentimentScreeningBacktest(
    symbols=["AAPL", "NVDA", "TSLA", "MSFT", "GOOGL"],
    start_date=date(2025, 1, 1),
    end_date=date(2025, 1, 31),
    initial_capital=Decimal("100000"),
    sentiment_threshold=0.2,        # 감성 점수 임계값
    sentiment_count_min=10,         # 최소 기사 수
    holding_days=5,                 # 최대 보유 기간
    stop_loss_pct=-3.0,             # Stop Loss
    take_profit_pct=5.0             # Take Profit
)

# 백테스트 실행
backtest.run()

# 결과 예시:
"""
💰 자본:
   - 초기 자본: $100,000.00
   - 최종 자본: $105,240.00
   - 총 수익: $5,240.00 (+5.24%)

📈 수익률:
   - CAGR: +65.2%
   - MDD: -2.8%

📊 거래 통계:
   - 총 거래 수: 15
   - 승리: 12 (80.0%)
   - 평균 수익: $349.33 (+3.5%)
   - Profit Factor: 3.2
"""
```

---

## 9. 성능 및 확장성

### 9.1 성능 최적화

#### 1. 인덱스 전략

```javascript
// MongoDB 인덱스 생성
db.daily_stock_data.createIndex({ date: 1 });
db.sentiment_analysis.createIndex({ ticker: 1, date: 1 });
db.stock_recommendations.createIndex({ ticker: 1, date: 1 });
```

**효과**:
- 날짜 범위 쿼리 성능 향상 (O(log n))
- Left Join 성능 향상 (dict lookup O(1))

#### 2. 버퍼링 전략

```python
# 지표 계산을 위한 추가 버퍼 (이동평균 등)
loader = MongoDataLoader(add_buffer_days=50)

# 실제 로드: start_date - 50일 ~ end_date
data = loader.load(
    symbols=["AAPL"],
    start_date=date(2025, 1, 1),  # 내부적으로 2024-11-12부터 로드
    end_date=date(2025, 1, 31)
)
```

**효과**:
- 이동평균 등 기술적 지표 정확도 향상
- 백테스트 초반 warm-up 기간 확보

#### 3. 메모리 관리

```python
# 종목별 개별 로드 (메모리 절약)
for symbol in large_symbol_list:
    df = loader.load_single(symbol, start_date, end_date)
    process(df)
    del df  # 메모리 해제
```

### 9.2 확장성

#### 데이터 볼륨 예상

| 컬렉션 | 일별 문서 수 | 문서 크기 | 1년 데이터 크기 |
|--------|--------------|-----------|-----------------|
| daily_stock_data | 1 | ~50KB (35개 종목) | ~18MB |
| sentiment_analysis | 35 | ~1KB | ~13MB |
| stock_recommendations | 35 | ~1KB | ~13MB |
| **합계** | **71** | - | **~44MB** |

#### 스케일링 전략

1. **수평 확장**: MongoDB 샤딩 (ticker 기준)
2. **시계열 파티셔닝**: 연도별 컬렉션 분리
3. **캐싱**: Redis를 통한 자주 사용되는 데이터 캐싱

---

## 10. 향후 계획

### Phase 1: 추가 지표 통합 (✅ 완료)

- [x] 감성 분석 데이터 통합
- [x] 기술적 추천 데이터 통합
- [x] Left Join 방식 구현
- [x] 레거시 데이터 처리

### Phase 2: 실시간 데이터 지원 (진행 중)

- [ ] Kafka 스트리밍 데이터 연동
- [ ] 실시간 감성 분석 파이프라인
- [ ] 실시간 기술적 지표 계산

### Phase 3: 성능 최적화 (예정)

- [ ] MongoDB 샤딩 구성
- [ ] Redis 캐싱 레이어
- [ ] 병렬 데이터 로딩
- [ ] 메모리 맵 파일 캐싱

### Phase 4: AI/ML 통합 (예정)

- [ ] Vertex AI 예측 지표 통합
- [ ] 강화학습 기반 포트폴리오 최적화
- [ ] AutoML 전략 탐색

---

## 📚 관련 문서

### 아키텍처
- [백테스트 시스템](../../architecture/백테스트_시스템.md) - 시스템 아키텍처 다이어그램

### 전략 시스템
- [전략 관리 시스템](../features/전략_관리_시스템.md) - 전략 CRUD 및 백테스트 실행
- [종목선정 이원화](../core/종목선정.md) - SCREENING vs PORTFOLIO
- [유니버스 분리](../core/유니버스_분리.md) - 전략-유니버스 분리 아키텍처

### 검증
- [백테스트 테스트 리포트](../testing/백테스트_테스트_리포트.md) - 테스트 결과

### 데이터베이스
- [MongoDB 스키마](../../database/스키마.md) - MongoDB 컬렉션 구조
- [PostgreSQL 스키마](../../../../docs/개체관계도.md) - PostgreSQL 테이블 구조

### 프로젝트 문서
- [PRD v2.0](../../../../docs/제품요구사항_버전2.md) - 제품 요구사항

---

**구현 위치**: `quant-jump-stock-data-engine/src/application/backtest/data_loader_mongo.py`
**테스트 코드**: `quant-jump-stock-data-engine/tests/test_mongo_data_loader_integration.py`
**실전 예시**: `quant-jump-stock-data-engine/scripts/backtest_sentiment_strategy.py`
**관련 이슈**: SCRUM-324, SCRUM-269 (전략 종목선정 이원화)

**작성**: Backend Team
**최종 업데이트**: 2026-02-13
**버전**: 2.0 (통합)
