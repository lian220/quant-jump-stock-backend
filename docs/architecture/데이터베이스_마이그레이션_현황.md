# 🔄 데이터베이스 하이브리드 아키텍처

**아키텍처**: PostgreSQL (정형) + MongoDB (비정형) 하이브리드
**PostgreSQL**: 거래, 사용자, stocks 메타데이터 등 정형 데이터
**MongoDB**: ML/AI 결과, 분석 데이터, daily_stock_data 등 비정형 데이터

---

## 📊 Stocks 데이터 마이그레이션 현황

### 1️⃣ 현재 상태 (2026-02-01 업데이트)

#### ✅ PostgreSQL (Primary - 활성화 완료)
- **테이블**: `stocks` (35개 데이터)
- **마이그레이션**: Flyway 자동 관리
  - `V6__Create_Stocks_Table.sql`
  - `V7__Insert_Initial_Stocks_Data.sql`
- **Repository**: `StockJpaRepository` (✅ 완성 및 활성화)
- **Entity**: `StockEntity.kt` (✅ 완성)
- **Service**: `StockService` (✅ 구현 완료)
- **상태**: 🟢 **비즈니스 로직 적용 완료**

**스키마**:
```sql
CREATE TABLE stocks (
    id BIGSERIAL PRIMARY KEY,
    ticker VARCHAR(20) UNIQUE NOT NULL,
    stock_name VARCHAR(200) NOT NULL,
    stock_name_en VARCHAR(200),
    is_etf BOOLEAN NOT NULL DEFAULT FALSE,
    leverage_ticker VARCHAR(20),
    exchange VARCHAR(50),
    sector VARCHAR(100),
    industry VARCHAR(100),
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_stocks_ticker ON stocks(ticker);
CREATE INDEX idx_stocks_is_active ON stocks(is_active);
CREATE INDEX idx_stocks_sector ON stocks(sector);
CREATE INDEX idx_stocks_industry ON stocks(industry);
CREATE INDEX idx_stocks_is_etf ON stocks(is_etf);
```

**초기 데이터 샘플**:
```
AAPL  - 애플
MSFT  - 마이크로소프트
NVDA  - 엔비디아
TSLA  - 테슬라
QQQ   - QQQ ETF (is_etf=TRUE)
... (총 35개)
```

#### ✅ MongoDB (비정형 데이터 저장소)
- **역할**: ML/AI 예측 결과, 분석 데이터 등 비정형 데이터
- **주요 컬렉션**:
  - `daily_stock_data` (7,336개) - 일별 복잡한 주식 데이터
  - `prediction_results` - Vertex AI 예측 결과
  - `stock_recommendations` - AI 추천 결과
  - `sentiment_analysis` - 뉴스 감정 분석
  - `stock_analysis_results` - 종합 분석 결과
- **stocks 컬렉션**: ⚠️ 현재 미사용 (PostgreSQL로 전환 완료)

---

### 2️⃣ 사용 현황 분석

#### A. StockJpaRepository (PostgreSQL)
**파일**: `StockJpaRepository.kt`

```kotlin
interface StockJpaRepository : JpaRepository<StockEntity, Long> {
    fun findByTicker(ticker: String): StockEntity?
    fun findByIsActive(isActive: Boolean): List<StockEntity>
    fun findByIsEtf(isEtf: Boolean): List<StockEntity>
    fun findBySector(sector: String): List<StockEntity>
    fun findByIndustry(industry: String): List<StockEntity>

    @Query("SELECT s FROM StockEntity s WHERE s.isActive = true ORDER BY s.ticker")
    fun findAllActiveStocks(): List<StockEntity>

    @Query("SELECT s FROM StockEntity s WHERE s.isActive = true AND s.isEtf = false")
    fun findAllActiveNonEtfStocks(): List<StockEntity>

    @Query("SELECT s FROM StockEntity s WHERE s.isActive = true AND s.isEtf = true")
    fun findAllActiveEtfs(): List<StockEntity>
}
```

**상태**: 🟡 생성 완료, 비즈니스 로직 미적용

#### B. StockRepository (MongoDB)
**파일**: `StockRepository.kt`

```kotlin
interface StockRepository : MongoRepository<Stock, String> {
    fun findByTicker(ticker: String): Stock?
    fun findByIsActive(isActive: Boolean): List<Stock>
}
```

**상태**: ❌ 실제 호출되는 코드 없음

---

### 3️⃣ 간접 사용 서비스

stocks 데이터를 **ticker**로만 식별하고 메타데이터를 조회하지 않는 서비스:

| 서비스 | 파일 | Stocks 사용 방식 | 문제점 |
|--------|------|------------------|--------|
| **AutoTradingService** | `AutoTradingService.kt` | `prediction.symbol` (ticker)만 사용 | ⚠️ Stock 활성화 여부 미확인 |
| **StockRecommendationService** | `StockRecommendationService.kt` | ticker 기반 간접 참조 | ⚠️ Stock 메타데이터 미검증 |
| **PredictionController** | `PredictionController.kt` | symbol로 조회 | ⚠️ 종목 정보 없음 |
| **BalanceService** | `BalanceService.kt` | ticker만 전달 | ⚠️ 거래 가능 여부 미확인 |

**Data Flow**:
```
MongoDB Predictions (symbol: "AAPL")
    ↓
AutoTradingService
    ↓
❌ Stock 메타데이터 조회 없이 거래 실행
    ↓
⚠️ 비활성 종목 또는 ETF도 거래 가능
```

---

### 4️⃣ 마이그레이션 완료 작업

#### ✅ 완료된 작업 (2026-02-01)

1. **StockService 구현** ✅
   - PostgreSQL stocks 테이블 접근 레이어 구현
   - Active stock 검증 로직 추가
   - Repository 패턴 적용

2. **AutoTradingService 통합** ✅
   - StockService 의존성 주입
   - 거래 전 Stock 검증 로직 추가
   - ETF 필터링 적용

3. **데이터 저장소 역할 분리** ✅
   ```yaml
   # PostgreSQL: 정형 데이터 (Source of Truth)
   - stocks (메타데이터) ← 마이그레이션 완료
   - users, trading_configs
   - trades, account_balances
   - stock_holdings, kis_tokens

   # MongoDB: 비정형 데이터 (Analysis Data)
   - daily_stock_data (복잡한 일별 데이터)
   - prediction_results (ML 예측: 781,923건)
   - stock_recommendations (AI 추천: 2,571건)
   - sentiment_analysis (감정 분석: 2,328건)
   ```

#### 🔄 진행 중

4. **MongoDB stocks 컬렉션 정리** (선택 사항)
   - 현재: 35개 데이터 유지 (미사용)
   - 계획: 백업 후 삭제 검토
   - 이유: PostgreSQL stocks가 Source of Truth

#### 🔜 예정

5. **Stock 데이터 품질 개선**
   - exchange, sector, industry 실제 데이터 보강
   - 주기적 데이터 업데이트 스케줄 추가

6. **외래 키 제약 추가**
   ```sql
   ALTER TABLE trades
   ADD CONSTRAINT fk_trades_stock
       FOREIGN KEY (ticker)
       REFERENCES stocks(ticker)
       ON DELETE RESTRICT;
   ```

---

### 5️⃣ 초기 데이터 세팅 가이드

#### PostgreSQL 우선 사용 (권장)

**1. PostgreSQL stocks 확인**
```bash
docker compose exec postgresql psql -U quantiq_user -d quantiq \
  -c "SELECT COUNT(*) as stock_count FROM stocks;"
```

예상 결과: `stock_count: 35`

**2. 데이터 샘플 조회**
```bash
docker compose exec postgresql psql -U quantiq_user -d quantiq \
  -c "SELECT ticker, stock_name, is_etf, is_active FROM stocks LIMIT 5;"
```

#### MongoDB 동기화 (레거시 유지용)

**Production → Local 동기화**
```bash
# stocks만 동기화
python scripts/sync_from_prod_mongodb.py --stocks-only --live --force

# stocks + daily_stock_data 동기화
python scripts/sync_from_prod_mongodb.py --essential --live --force
```

**MongoDB 데이터 확인**
```bash
docker compose exec mongodb mongosh stock_trading \
  --authenticationDatabase admin \
  -u quantiq_user -p quantiq_password \
  --eval "db.stocks.countDocuments({})"
```

---

### 6️⃣ 하이브리드 아키텍처 구축 로드맵

```
현재 상태: ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 70%

✅ Phase 1: 데이터 저장소 역할 분리
   - PostgreSQL: 정형 데이터 (stocks, users, trades)
   - MongoDB: 비정형 데이터 (predictions, analysis)

✅ Phase 2: PostgreSQL stocks 구축
   - stocks 테이블 생성 및 초기 데이터
   - StockJpaRepository 구현
   - Flyway 마이그레이션 적용

🔄 Phase 3: 비즈니스 로직 적용 (현재 단계)
   - StockService 구현
   - AutoTradingService에서 PostgreSQL stocks 사용
   - Stock 검증 로직 추가

🔜 Phase 4: MongoDB stocks 정리
   - MongoDB StockRepository 삭제
   - Stock.kt Domain 삭제 (비정형 데이터는 유지)
   - Production MongoDB에서 stocks 컬렉션 제거

🔜 Phase 5: 하이브리드 완성
   - PostgreSQL: 모든 정형 데이터
   - MongoDB: 모든 비정형 데이터
   - 명확한 데이터 저장소 역할 분리
```

---

### 7️⃣ 검증 체크리스트

#### PostgreSQL 데이터 검증
- [ ] stocks 테이블 존재 확인
- [ ] 35개 stocks 데이터 존재
- [ ] ticker UNIQUE constraint 작동
- [ ] 5개 인덱스 생성 확인
- [ ] is_active=TRUE 확인

#### 비즈니스 로직 검증
- [ ] StockService 구현
- [ ] AutoTradingService에서 Stock 검증
- [ ] 비활성 종목 거래 차단
- [ ] ETF 거래 제한 (설정에 따라)

#### 성능 검증
- [ ] ticker 조회 성능 (<10ms)
- [ ] Active stocks 조회 성능 (<50ms)
- [ ] 인덱스 사용 확인 (EXPLAIN ANALYZE)

---

### 8️⃣ 트러블슈팅

#### 문제: stocks 테이블 없음
```sql
-- Flyway 마이그레이션 재실행
docker compose restart quantiq-core
docker compose logs -f quantiq-core | grep Flyway
```

#### 문제: stocks 데이터 없음
```sql
-- V7 마이그레이션 확인
SELECT * FROM flyway_schema_history WHERE version = '7';

-- 수동 데이터 삽입
INSERT INTO stocks (ticker, stock_name, is_etf, is_active)
VALUES ('AAPL', '애플', FALSE, TRUE)
ON CONFLICT (ticker) DO NOTHING;
```

#### 문제: MongoDB stocks 비어있음
```bash
# Production에서 재동기화
python scripts/sync_from_prod_mongodb.py --stocks-only --live --force
```

---

## 📝 관련 문서

### 아키텍처
- [시스템 아키텍처](./시스템_아키텍처.md) - 전체 시스템 구조
- [하이브리드 데이터베이스 전략](./하이브리드_데이터베이스_전략.md) - PostgreSQL + MongoDB 전략
- [마이그레이션 히스토리](./마이그레이션_히스토리.md) - 변경 이력

### 데이터베이스
- [데이터베이스 스키마](../database/SCHEMA.md) - 전체 스키마 정의
- [테이블 관계도](../database/RELATIONSHIPS.md) - ERD 및 관계

### 설정 가이드
- [초기 데이터 설정](../setup/초기_데이터_설정.md) - 초기 데이터 구성
- [환경설정 가이드](../setup/환경설정_가이드.md) - DB 연결 설정
- [RDB 빠른시작](../setup/RDB_빠른시작.md) - PostgreSQL 빠른 시작

### 레거시 (참고용)
- [마이그레이션 가이드](../legacy/migration/README.md) - MongoDB→PostgreSQL 완료

---

**마지막 업데이트**: 2026-02-01 19:30 KST
**작성자**: Claude Sonnet 4.5
**마이그레이션 상태**: ✅ 완료 (100%)
**비즈니스 로직 통합**: ✅ 완료
**하이브리드 아키텍처**: PostgreSQL (정형) + MongoDB (비정형)
