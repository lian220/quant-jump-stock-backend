# 스케줄러 아키텍처

**목적**: QuantIQ 스케줄러 시스템의 구조와 작동 방식 설명
**대상**: 개발자, 시스템 관리자
**작성일**: 2026-01-31
**최종 업데이트**: 2026-02-01 (v2.0 - Vertex AI 통합 및 스케줄 최적화)

---

## 📋 목차

1. [개요](#개요)
2. [아키텍처 구조](#아키텍처-구조)
3. [Quartz 스케줄러](#quartz-스케줄러)
4. [Job 실행 흐름](#job-실행-흐름)
5. [등록된 Job](#등록된-job)
6. [데이터 저장소](#데이터-저장소)
7. [이벤트 기반 통합](#이벤트-기반-통합)

---

## 개요

QuantIQ 스케줄러는 **Quartz Scheduler**를 기반으로 하며, **Hexagonal Architecture** 패턴을 적용하여 구현되었습니다.

### 주요 특징

- **정기 작업 자동화**: 경제 데이터 업데이트, 병렬 분석 등을 자동으로 실행
- **유연한 스케줄 관리**: Cron 표현식을 사용한 정밀한 시간 제어
- **영속성 지원**: PostgreSQL에 스케줄 상태 저장 (애플리케이션 재시작 시에도 유지)
- **클러스터링 지원**: 분산 환경에서 안전한 Job 실행
- **REST API**: HTTP API를 통한 스케줄러 제어 (pause/resume/start/stop)

### 기술 스택

| 구성 요소 | 기술 |
|----------|------|
| 스케줄러 | Quartz Scheduler 2.3+ |
| 언어 | Kotlin |
| 프레임워크 | Spring Boot 3.x |
| 아키텍처 | Hexagonal Architecture |
| 저장소 | PostgreSQL |
| 타임존 | Asia/Seoul (KST) |

---

## 아키텍처 구조

### Hexagonal Architecture 적용

```
┌─────────────────────────────────────────────────────────────┐
│                      Input Adapters                         │
├─────────────────────────────────────────────────────────────┤
│  SchedulerRestController    EconomicDataUpdateJobAdapter    │
│  (REST API)                 (Quartz Job)                    │
└────────────────┬─────────────────────┬──────────────────────┘
                 │                     │
                 ↓                     ↓
┌─────────────────────────────────────────────────────────────┐
│                   Domain Layer (Port)                       │
├─────────────────────────────────────────────────────────────┤
│  Input Port:  SchedulerUseCase                              │
│  Output Port: SchedulerRepository                           │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────────────────────────┐
│                  Application Layer                          │
├─────────────────────────────────────────────────────────────┤
│  SchedulerManagementService (UseCase 구현)                  │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────────────────────────┐
│                   Output Adapters                           │
├─────────────────────────────────────────────────────────────┤
│  QuartzSchedulerAdapter (Repository 구현)                   │
│  ↓                                                           │
│  Quartz Scheduler → PostgreSQL                              │
└─────────────────────────────────────────────────────────────┘
```

### 주요 컴포넌트

#### Input Adapters
- **SchedulerRestController**: HTTP 요청을 받아 UseCase로 전달
  - 경로: `/api/scheduler/*`
  - 기능: 상태 조회, 스케줄 관리, 시작/중지
- **EconomicDataUpdateJobAdapter**: Quartz 트리거를 받아 경제 데이터 업데이트 실행
- **ParallelAnalysisJob**: Quartz 트리거를 받아 병렬 분석 실행

#### Domain Layer (Port)
- **SchedulerUseCase** (Input Port): 스케줄러 관리 비즈니스 로직 인터페이스
- **SchedulerRepository** (Output Port): 스케줄러 저장소 인터페이스

#### Application Layer
- **SchedulerManagementService**: UseCase 구현체, 비즈니스 로직 처리

#### Output Adapters
- **QuartzSchedulerAdapter**: SchedulerRepository 구현, Quartz와 연동

---

## Quartz 스케줄러

### 핵심 개념

| 개념 | 설명 | 예시 |
|------|------|------|
| **Job** | 실행할 작업 | `EconomicDataUpdateJobAdapter` |
| **JobDetail** | Job의 메타데이터 | Job 이름, 설명, 클래스 |
| **Trigger** | 실행 시점 정의 | Cron 표현식 기반 트리거 |
| **Scheduler** | Job과 Trigger 관리 | Quartz Scheduler 인스턴스 |

### Cron 표현식 구조

```
초(0-59) 분(0-59) 시(0-23) 일(1-31) 월(1-12) 요일(0-7) [년도]
```

**예시:**
```kotlin
"0 5 6 * * ?"      // 매일 06:05:00 실행
"0 5 23 * * ?"     // 매일 23:05:00 실행
"0 0 12 * * MON"   // 매주 월요일 12:00 실행
```

### QuartzConfig.kt 구조

```kotlin
@Configuration
class QuartzConfig {

    // 1. JobDetail 생성 (Job 메타데이터)
    @Bean
    fun economicDataUpdateJobDetail(): JobDetail {
        return JobBuilder.newJob(EconomicDataUpdateJobAdapter::class.java)
            .withIdentity("economicDataUpdateJob")
            .storeDurably()  // 트리거 없어도 유지
            .build()
    }

    // 2. Trigger 생성 (실행 시점)
    @Bean
    fun economicDataUpdateTrigger(): Trigger {
        return TriggerBuilder.newTrigger()
            .forJob(economicDataUpdateJobDetail())
            .withIdentity("economicDataUpdateTrigger")
            .withSchedule(
                CronScheduleBuilder.cronSchedule("0 5 6 * * ?")
                    .inTimeZone(TimeZone.getTimeZone("Asia/Seoul"))
            )
            .build()
    }
}
```

---

## Job 실행 흐름

### 1. 스케줄 기반 실행 (Cron)

```
시간 도달 (예: 06:05)
    ↓
Quartz Scheduler 트리거 발동
    ↓
Job Adapter 실행 (예: EconomicDataUpdateJobAdapter)
    ↓
UseCase 호출 (예: economicDataUseCase.triggerEconomicDataUpdate())
    ↓
이벤트 발행 (Kafka: quantiq.economic.data.request)
    ↓
Data Engine 처리
    ↓
완료 이벤트 수신 (Kafka: quantiq.economic.data.completed)
    ↓
로그 기록 및 완료
```

### 2. REST API를 통한 수동 실행

```
HTTP Request (POST /api/scheduler/start)
    ↓
SchedulerRestController
    ↓
SchedulerManagementService (UseCase)
    ↓
QuartzSchedulerAdapter (Repository)
    ↓
Quartz Scheduler 시작
    ↓
HTTP Response (success: true)
```

### 3. Job 실행 라이프사이클

```kotlin
// Job 인터페이스 구현
class EconomicDataUpdateJobAdapter : Job {

    override fun execute(context: JobExecutionContext?) {
        try {
            logger.info("경제 데이터 업데이트 시작")

            // 1. UseCase 호출
            economicDataUseCase.triggerEconomicDataUpdate()
                .thenAccept { result ->
                    logger.info("✅ 업데이트 완료: $result")
                }
                .exceptionally { e ->
                    logger.error("❌ 업데이트 실패", e)
                    null
                }
                .get()  // 비동기 완료 대기

            logger.info("경제 데이터 업데이트 종료")
        } catch (e: Exception) {
            logger.error("Job 실행 중 오류", e)
            throw JobExecutionException(e)  // Quartz에 실패 알림
        }
    }
}
```

---

## 등록된 Job

### 1. 경제 데이터 업데이트 + Vertex AI 예측 (EconomicDataUpdate2JobAdapter)

| 속성 | 값 |
|------|-----|
| **Job 이름** | `economicDataUpdate2Job` |
| **Trigger 이름** | `economicDataUpdate2Trigger` |
| **실행 시간** | 매일 22:00 (KST) |
| **Cron 표현식** | `0 0 22 * * ?` |
| **역할** | 경제 데이터 수집 + Vertex AI 예측 실행 |
| **이벤트 발행** | `quantiq.economic.data.request` |
| **구현 상태** | ✅ 구현 완료 |

**실행 타이밍 근거:**
- **CPI/NFP 발표**: 21:30 KST (08:30 ET) - 주요 경제 지표
- **ISM PMI 발표**: 23:00 KST (10:00 ET) - 제조업 지표
- **22:00 실행**: CPI/NFP 발표 직후, ISM PMI 발표 직전
- **Vertex AI Fine-tuning**: 3-5분 소요 (기존 25-30분 대비 83% 단축)

**실행 흐름:**
```
22:00 → EconomicDataUpdate2JobAdapter
      → economicDataUseCase.triggerEconomicDataUpdate()
      → Kafka 이벤트 발행 (경제 데이터 수집)
      → Data Engine 처리
      → runVertexAIPrediction()
      → Python 스크립트 (run_predict_vertex_ai.py)
      → Google Vertex AI CustomJob 생성
      → GPU 기반 모델 Fine-tuning (5 epochs)
      → GCS에 모델/Scaler 저장
      → 예측 결과 MongoDB 저장
```

**Vertex AI 통합:**
- **플랫폼**: Google Cloud Vertex AI Custom Training Jobs
- **모델**: Transformer (2-layer, Multi-head Attention)
- **GPU**: NVIDIA Tesla T4 (기본), L4 (DWS 지원)
- **저장소**: GCS (models/transformer_stock_model.h5)
- **Fine-tuning 모드**: 기존 모델 로드 → 5 epochs 학습 (vs 전체 학습 50 epochs)
- **데이터**: 최근 24개월 데이터만 로드 (메모리 최적화)
- **예상 비용**: ~$0.03/day (~$1/month)

### 2. 병렬 분석 (ParallelAnalysisJob) - 검증용

| 속성 | 값 |
|------|-----|
| **Job 이름** | `parallelAnalysisJob` |
| **Trigger 이름** | `parallelAnalysisTrigger` |
| **실행 시간** | 매일 23:05 (KST) |
| **Cron 표현식** | `0 5 23 * * ?` |
| **역할** | Vertex AI 예측 결과 검증 (기술적 지표 + 감정 분석) |
| **구현 상태** | ⚠️ 스켈레톤만 존재 (TODO) |

**실행 타이밍 근거:**
- **ISM PMI 발표 후**: 23:00 ISM PMI 발표 직후 실행
- **예측 검증**: 22:00의 Vertex AI 예측 결과를 기술적 지표로 검증
- **매수 준비**: 00:30 자동 매수 전 최종 검증 단계

**현재 구현:**
```kotlin
override fun execute(context: JobExecutionContext?) {
    logger.info("병렬 분석 시작 (23:05)")

    // TODO: 병렬 분석 로직 구현
    // - 기술적 지표 분석
    // - 감정 분석

    logger.info("병렬 분석 완료")
}
```

**예정된 구현:**
- Vertex AI 예측 결과 조회
- 기술적 지표 분석 서비스 호출 (검증용)
- 감정 분석 서비스 호출 (검증용)
- Kafka 이벤트 발행
- 검증 결과 저장 및 매수 신호 생성

### 전체 Job 목록

| # | Job 이름 | 실행 시간 | 역할 | 상태 |
|---|----------|----------|------|------|
| 1 | economicDataUpdate2Job | 22:00 (매일) | 경제 데이터 수집 + Vertex AI 예측 | ✅ 완료 |
| 2 | parallelAnalysisJob | 23:05 (매일) | 예측 검증 (기술적 지표 + 감정 분석) | ⚠️ TODO |
| 3 | autoBuyJob | 00:30 (매일) | 자동 매수 실행 (장 개장 1시간 후) | ✅ 완료 |
| 4 | combinedAnalysisJob | 01:30 (매일) | 매수 후 검증 (기술 70% + 감정 30%) | ✅ 완료 |
| 5 | sentimentAnalysisJob | 02:30 (매일) | 뉴스 감정 분석 (매수 결정 재검증) | ⚠️ TODO |
| 6 | cleanupOrdersJob | 06:30 (매일) | 미체결 주문 정리 | ✅ 완료 |
| 7 | portfolioProfitReportJob | 07:00 (매일) | 포트폴리오 수익률 보고 | ✅ 완료 |
| 8 | autoSellJob | **매 1분** | 자동 매도 체크 (미국 시장 시간만) | ✅ 완료 |

**스케줄 최적화 근거:**
- **22:00**: CPI/NFP(21:30) 직후, ISM PMI(23:00) 직전 - 주요 지표 발표 사이
- **23:05**: ISM PMI(23:00) 직후 - 모든 경제 지표 수집 완료 후 검증
- **00:30**: 미국 장 개장(23:30) 1시간 후 - 초기 변동성 안정화 후 매수
  - 통계적 근거: 첫 30분간 일일 최고가의 24% 발생, 1시간 후 안정화
- **01:30**: 매수 1시간 후 - 기술적 분석으로 매수 결정 검증
- **02:30**: 매수 2시간 후 - 뉴스 감정 분석으로 최종 검증

#### 3. 자동 매수 (AutoBuyJobAdapter)

| 속성 | 값 |
|------|-----|
| **Job 이름** | `autoBuyJob` |
| **Trigger 이름** | `autoBuyTrigger` |
| **실행 시간** | 매일 00:30 (KST) |
| **Cron 표현식** | `0 30 0 * * ?` |
| **역할** | Vertex AI 예측 기반 자동 매수 실행 |
| **구현 상태** | ✅ 구현 완료 |

**실행 타이밍 근거:**
- **미국 정규장 개장**: 23:30 KST (09:30 ET)
- **00:30 실행**: 개장 1시간 후
- **변동성 패턴**:
  - 첫 30분: 일일 최고가의 24% 발생 (가장 높은 변동성)
  - 30분~1시간: 변동성 감소, 트렌드 확정 시작
  - 1시간 이후: 안정적인 트렌드 진입 구간
- **안전한 진입**: 초기 변동성 회피, 확정된 트렌드에서 매수

**실행 흐름:**
```
00:30 → AutoBuyJobAdapter
      → AutoTradingUseCase.executeAutoBuy()
      → Vertex AI 예측 결과 조회
      → 매수 신호 필터링 (신뢰도 임계값)
      → KIS API 매수 주문 실행
      → 주문 결과 저장 및 알림
```

#### 8. 자동 매도 체크 (AutoSellJobAdapter)

| 속성 | 값 |
|------|-----|
| **Job 이름** | `autoSellJob` |
| **Trigger 이름** | `autoSellTrigger` |
| **실행 시간** | **매 1분마다 (반복)** |
| **스케줄 타입** | SimpleSchedule |
| **역할** | 실시간 매도 조건 확인 및 실행 |
| **시장 시간 검증** | 미국 시장 시간만 (평일 9:30 AM - 4:00 PM ET) |
| **구현 상태** | ✅ 구현 완료 (2026-02-01) |

**실행 흐름:**
```
매 1분 → AutoSellJobAdapter
       → isUsMarketHours() 체크
       → (미국 시장 시간이면)
       → AutoTradingUseCase.checkAndExecuteSellOrders()
       → 매도 조건 확인 (손절/익절)
       → 매도 주문 실행
```

**미국 시장 시간 검증:**
```kotlin
private fun isUsMarketHours(): Boolean {
    val nyTime = ZonedDateTime.now(ZoneId.of("America/New_York"))
    val hour = nyTime.hour
    val minute = nyTime.minute
    val dayOfWeek = nyTime.dayOfWeek

    // 주말 제외
    if (dayOfWeek == DayOfWeek.SATURDAY || dayOfWeek == DayOfWeek.SUNDAY) {
        return false
    }

    // 9:30 AM - 4:00 PM ET
    if (hour < 9 || (hour == 9 && minute < 30) || hour >= 16) {
        return false
    }

    return true
}
```

**특징:**
- banbu-stocktrading 스타일 실시간 매도 모니터링
- 미국 시장 시간 외에는 건너뜀 (리소스 절약)
- 평일만 실행 (주말 제외)
- 매 1분마다 반복 실행 (SimpleSchedule)

---

## MongoDB 컬렉션 데이터 흐름

### 스케줄러별 데이터 저장 매핑

각 스케줄러가 어떤 MongoDB 컬렉션에 데이터를 저장하는지 정리합니다.

| 컬렉션 | 스케줄러 | 실행 시간 | 업데이트 주기 | 설명 |
|--------|---------|----------|--------------|------|
| `daily_stock_data` | economicDataUpdate2Job | 22:00 (매일) | 일일 | FRED + Yahoo Finance 통합 경제 지표 |
| `stock_predictions` | economicDataUpdate2Job | 22:00 (매일) | 일일 | Vertex AI 예측 결과 |
| `stock_recommendations` | combinedAnalysisJob | 01:30 (매일) | 일일 | 기술적 분석 기반 매수 추천 |
| `sentiment_analysis` | sentimentAnalysisJob | 02:30 (매일) | 일일 | 뉴스 감정 분석 결과 |
| `yfinance_indicators` | (설정 데이터) | - | 수동 | Yahoo Finance 지표 정의 |
| `fred_indicators` | (설정 데이터) | - | 수동 | FRED 지표 정의 |
| `access_tokens` | KIS API | 실시간 | 필요시 | KIS 액세스 토큰 캐시 |
| `stocks` | (마스터 데이터) | - | 수동 | 종목 정보 |

### 컬렉션 상태 확인 (2026-02-01 기준)

```bash
# MongoDB 컬렉션 데이터 건수 조회
docker compose exec -T mongodb mongosh -u quantiq_user -p quantiq_password \
  --authenticationDatabase admin stock_trading --eval "
    db.daily_stock_data.countDocuments({})      // 7,335건 (최신: 2026-01-30) ✅
    db.stock_predictions.countDocuments({})     // 781,923건 (최신: 2026-01-28) ⚠️
    db.stock_recommendations.countDocuments({}) // 2,572건 (최신: 2026-01-28) ⚠️
    db.yfinance_data.countDocuments({})         // 91건 (최신: 2026-01-02) ❌
    db.fred_data.countDocuments({})             // 38건 (최신: 2026-01-01) ❌
  " --quiet
```

### 주요 컬렉션 상세 설명

#### 1. daily_stock_data (경제 지표 통합)

**업데이트 스케줄러**: `economicDataUpdate2Job` (매일 22:00)

**데이터 구조**:
```json
{
  "_id": "697ea5af4e4e84c5314f95d4",
  "date": "2026-01-30",
  "fred_indicators": {
    "GDP": 28269.5,
    "Unemployment_Rate": 4.1,
    "Inflation_CPI": 318.5,
    "Treasury_10Y": 4.5,
    "USD_KRW": 1350.5
  },
  "yfinance_indicators": {
    "SP500": 6939.02,
    "Dow_Jones": 48892.46,
    "NASDAQ": 23461.82,
    "KOSPI": 5224.35,
    "Gold": 4713.89
  },
  "updated_at": "2026-02-01T10:00:31.251Z"
}
```

**업데이트 흐름**:
```
22:00 → EconomicDataUpdate2JobAdapter
      → economicDataUseCase.triggerEconomicDataUpdate()
      → Kafka: economic.data.update.request
      → Python Data Engine: EconomicDataService.collect_economic_data()
      → FRED API + Yahoo Finance 데이터 수집 (최근 365일)
      → MongoDB: daily_stock_data 저장
```

**데이터 수집 범위**:
- **FRED 지표**: GDP, 실업률, CPI, 10년물 국채 금리, USD/KRW 환율
- **Yahoo Finance 지표**: S&P500, Dow Jones, NASDAQ, KOSPI, Gold
- **기간**: 최근 365일 (분기별/월별/일별 지표 반영)

#### 2. stock_predictions (Vertex AI 예측)

**업데이트 스케줄러**: `economicDataUpdate2Job` (매일 22:00)

**데이터 구조**:
```json
{
  "_id": "697b539afbb7eeabaa3449ca",
  "date": "2026-01-28T00:00:00.000Z",
  "ticker": "GOOGL",
  "predicted_price": 336.91,
  "actual_price": 327.93,
  "forecast_horizon": 14,
  "created_at": "2026-01-28T14:16:08.933Z",
  "updated_at": "2026-01-28T14:16:08.933Z"
}
```

**업데이트 흐름**:
```
22:00 → EconomicDataUpdate2JobAdapter
      → runVertexAIPrediction()
      → Python: run_predict_vertex_ai.py
      → Vertex AI CustomJob 생성
      → Transformer 모델 Fine-tuning (5 epochs)
      → 예측 결과 → MongoDB: stock_predictions 저장
```

**예측 대상 종목**: S&P 500 상위 50개 종목
**예측 기간**: 14일 후 종가

#### 3. stock_recommendations (매수 추천)

**업데이트 스케줄러**: `combinedAnalysisJob` (매일 01:30)

**데이터 구조**:
```json
{
  "_id": "697b5389fbb7eeabaa304fca",
  "date": "2026-01-28",
  "ticker": "AAPL",
  "is_recommended": true,
  "recommendation_score": 0.85,
  "technical_indicators": {
    "sma20": 150.5,
    "sma50": 145.2,
    "golden_cross": true,
    "rsi": 65.3,
    "macd": 2.5,
    "signal": 1.8,
    "macd_buy_signal": true
  },
  "user_id": "lian",
  "stock_id": "AAPL_001",
  "created_at": "2026-01-28T14:30:25.928Z",
  "updated_at": "2026-01-28T14:30:25.928Z"
}
```

**업데이트 흐름**:
```
01:30 → CombinedAnalysisJobAdapter
      → Kafka: analysis.combined.request
      → Python: TechnicalAnalysisService.analyze()
      → 기술적 지표 계산 (SMA, RSI, MACD)
      → MongoDB: stock_recommendations 저장
```

### 사용되지 않는 레거시 컬렉션

다음 컬렉션들은 현재 스케줄러에서 업데이트되지 않습니다:

| 컬렉션 | 상태 | 마지막 업데이트 | 권장 조치 |
|--------|------|-----------------|----------|
| `yfinance_data` | ⚠️ 사용 안 함 | 2026-01-02 | 제거 또는 아카이브 |
| `fred_data` | ⚠️ 사용 안 함 | 2026-01-01 | 제거 또는 아카이브 |

**이유**:
- 기존에는 `yfinance_data`와 `fred_data`에 원본 데이터를 저장했으나,
- 현재는 `daily_stock_data`에 통합하여 저장하므로 불필요
- 데이터 중복 방지 및 스토리지 최적화

### 수동 트리거를 통한 데이터 수집

스케줄러 대기 없이 즉시 데이터를 수집하려면 REST API를 사용:

```bash
# 경제 데이터 수집 (daily_stock_data 업데이트)
curl -X POST http://localhost:10010/api/economic/trigger-update

# 기술적 분석 (stock_recommendations 업데이트)
curl -X POST http://localhost:10010/api/analysis/trigger-technical

# 감정 분석
curl -X POST http://localhost:10010/api/analysis/trigger-sentiment

# 통합 분석
curl -X POST http://localhost:10010/api/analysis/trigger-combined

# 병렬 분석
curl -X POST http://localhost:10010/api/analysis/trigger-parallel
```

---

## 데이터 저장소

### PostgreSQL 테이블 구조

Quartz는 스케줄 정보를 PostgreSQL에 저장하여 영속성을 보장합니다.

#### 주요 테이블

| 테이블 | 역할 | 주요 컬럼 |
|--------|------|----------|
| `quartz_job_details` | Job 메타데이터 | job_name, job_class_name, is_durable |
| `quartz_triggers` | 트리거 정보 | trigger_name, next_fire_time, trigger_state |
| `quartz_cron_triggers` | Cron 트리거 | cron_expression, time_zone_id |
| `quartz_fired_triggers` | 실행 중인 Job | fired_time, state, instance_name |
| `quartz_scheduler_state` | 스케줄러 상태 | instance_name, last_checkin_time |
| `quartz_locks` | 분산 락 | lock_name |

#### 테이블 관계도

```
quartz_job_details (부모)
    ↓ FK
quartz_triggers
    ↓ FK
quartz_cron_triggers (Cron 타입일 때)
quartz_simple_triggers (Simple 타입일 때)
quartz_simprop_triggers (Property 타입일 때)
```

#### 마이그레이션 파일

- **위치**: `quantiq-core/src/main/resources/db/migration/V3__Create_Quartz_Tables.sql`
- **적용 시점**: Flyway가 애플리케이션 시작 시 자동 실행
- **버전**: V3 (경제 데이터 테이블 이후)

### 스케줄 조회 쿼리 예시

```sql
-- 모든 스케줄 조회
SELECT
    t.trigger_name,
    t.job_name,
    t.next_fire_time,
    t.prev_fire_time,
    t.trigger_state,
    ct.cron_expression,
    ct.time_zone_id
FROM quartz_triggers t
LEFT JOIN quartz_cron_triggers ct
    ON t.trigger_name = ct.trigger_name;

-- 실행 예정 시간 확인
SELECT
    trigger_name,
    to_timestamp(next_fire_time / 1000) AT TIME ZONE 'Asia/Seoul' as next_run,
    trigger_state
FROM quartz_triggers
WHERE trigger_state = 'WAITING'
ORDER BY next_fire_time;
```

---

## 이벤트 기반 통합

### Kafka 이벤트 연동

스케줄러는 **이벤트 기반 아키텍처**와 통합되어 있습니다.

#### 발행 이벤트 (Scheduler → Kafka)

| 이벤트 토픽 | 발행자 | 타이밍 | 페이로드 |
|------------|--------|--------|----------|
| `quantiq.economic.data.request` | EconomicDataUpdate2JobAdapter | 매일 22:00 | `{"requestedAt": "2026-02-01T22:00:00"}` |
| `quantiq.analysis.request` | ParallelAnalysisJob (예정) | 매일 23:05 | `{"type": "VALIDATION", "requestedAt": "..."}` |
| `quantiq.trading.buy.executed` | AutoBuyJobAdapter | 매일 00:30 | `{"symbol": "AAPL", "quantity": 10, "price": 150.0}` |

#### 구독 이벤트 (Kafka → Scheduler)

| 이벤트 토픽 | 구독자 | 처리 내용 |
|------------|--------|----------|
| `quantiq.economic.data.completed` | KafkaMessageListener | 경제 데이터 업데이트 완료 처리 |
| `quantiq.analysis.completed` | KafkaMessageListener | 분석 완료 처리 (AutoTrading 트리거) |

### 이벤트 흐름 예시

**전체 자동 매매 플로우 (일일 주기):**
```
[22:00] 경제 데이터 수집 + Vertex AI 예측
    ↓
EconomicDataUpdate2JobAdapter.execute()
    ↓
1) economicDataUseCase.triggerEconomicDataUpdate()
    → Kafka: quantiq.economic.data.request
    → Data Engine: 경제 데이터 수집 (FRED, Yahoo Finance)
    → MongoDB: daily_stock_data 업데이트
    ↓
2) runVertexAIPrediction()
    → Python: run_predict_vertex_ai.py
    → Vertex AI: CustomJob 생성
    → GPU Fine-tuning (3-5분)
    → GCS: 모델/Scaler 저장
    → MongoDB: 예측 결과 저장
    ↓
[23:05] 예측 검증 (기술적 지표)
    → ParallelAnalysisJob (TODO)
    → 기술적 분석으로 예측 검증
    ↓
[00:30] 자동 매수
    → AutoBuyJobAdapter
    → Vertex AI 예측 결과 조회
    → 매수 신호 필터링
    → KIS API 매수 주문
    → Slack 알림
    ↓
[01:30] 매수 후 검증
    → CombinedAnalysisJobAdapter
    → 기술적 분석 (70%) + 감정 분석 (30%)
    → 매수 결정 재평가
    ↓
[02:30] 감정 분석 재검증
    → SentimentAnalysisJob (TODO)
    → 뉴스 감정 분석
    → 매수 유지/취소 판단
    ↓
[매 1분] 자동 매도 (미국 시장 시간)
    → AutoSellJobAdapter
    → 손절/익절 조건 체크
    → 매도 주문 실행
```

### 이벤트 스키마

자세한 이벤트 스키마는 [EVENT_SCHEMA.md](./EVENT_SCHEMA.md)를 참조하세요.

---

## 주요 클래스 참조

| 클래스 | 경로 | 역할 |
|--------|------|------|
| `QuartzConfig` | `scheduler/QuartzConfig.kt` | Job 및 Trigger 등록 |
| `EconomicDataUpdateJobAdapter` | `adapter/input/scheduler/` | 경제 데이터 업데이트 Job |
| `ParallelAnalysisJob` | `scheduler/ParallelAnalysisJob.kt` | 병렬 분석 Job |
| `SchedulerRestController` | `adapter/input/rest/` | REST API 엔드포인트 |
| `SchedulerManagementService` | `application/scheduler/` | UseCase 구현체 |
| `QuartzSchedulerAdapter` | `adapter/output/persistence/` | Quartz 연동 Adapter |
| `SchedulerUseCase` | `domain/scheduler/port/input/` | Input Port |
| `SchedulerRepository` | `domain/scheduler/port/output/` | Output Port |

---

## 설정 파일

### application.yml

```yaml
spring:
  quartz:
    job-store-type: jdbc          # PostgreSQL 사용
    jdbc:
      initialize-schema: never     # Flyway가 스키마 관리
    properties:
      org.quartz.scheduler.instanceName: QuantIQScheduler
      org.quartz.scheduler.instanceId: AUTO
      org.quartz.jobStore.class: org.quartz.impl.jdbcjobstore.JobStoreTX
      org.quartz.jobStore.driverDelegateClass: org.quartz.impl.jdbcjobstore.PostgreSQLDelegate
      org.quartz.jobStore.tablePrefix: quartz_
      org.quartz.jobStore.isClustered: true  # 클러스터링 지원
      org.quartz.threadPool.threadCount: 10
```

### Gradle 의존성

```kotlin
dependencies {
    implementation("org.springframework.boot:spring-boot-starter-quartz")
    implementation("org.postgresql:postgresql")
}
```

---

## 확장 가능성

### 새 Job 추가 방법

1. **Job 클래스 작성**
```kotlin
@Component
class NewAnalysisJob : Job {
    override fun execute(context: JobExecutionContext?) {
        // Job 로직
    }
}
```

2. **QuartzConfig에 등록**
```kotlin
@Bean
fun newAnalysisJobDetail(): JobDetail {
    return JobBuilder.newJob(NewAnalysisJob::class.java)
        .withIdentity("newAnalysisJob")
        .storeDurably()
        .build()
}

@Bean
fun newAnalysisTrigger(): Trigger {
    return TriggerBuilder.newTrigger()
        .forJob(newAnalysisJobDetail())
        .withSchedule(CronScheduleBuilder.cronSchedule("0 0 12 * * ?"))
        .build()
}
```

### 클러스터링 고려사항

- Quartz는 PostgreSQL 락을 사용하여 분산 환경에서 중복 실행 방지
- `quartz_locks` 테이블을 통한 분산 락 관리
- 여러 인스턴스가 동시에 실행되어도 안전

---

## 관련 문서

### 아키텍처
- [시스템 아키텍처](./시스템_아키텍처.md) - 전체 시스템 구조
- [이벤트 기반 아키텍처](./이벤트_기반_아키텍처.md) - Kafka 이벤트 통합
- [이벤트 스키마](./이벤트_스키마.md) - 이벤트 메시지 형식

### 설정 및 운영
- [스케줄러 운영 가이드](../setup/스케줄러_운영_가이드.md) - 스케줄 관리 및 운영
- [환경설정 가이드](../setup/환경설정_가이드.md) - Quartz 설정
- [배포 운영 가이드](../setup/배포_운영_가이드.md) - 프로덕션 배포

### 데이터베이스
- [데이터베이스 스키마](../database/SCHEMA.md) - Quartz 테이블 포함
- [하이브리드 데이터베이스 전략](./하이브리드_데이터베이스_전략.md) - PostgreSQL + MongoDB

### 외부 연동
- [KIS 토큰 관리](./KIS_토큰_관리.md) - 한국투자증권 API 토큰
- [Slack 설정 가이드](../setup/Slack_설정_가이드.md) - 알림 연동

---

**마지막 업데이트**: 2026-02-01
**버전**: 2.0 (Vertex AI 통합 및 스케줄 최적화)
**작성자**: QuantIQ Development Team

**주요 변경사항 (v2.0):**
- 경제 데이터 수집 시간 변경: 06:05 → 22:00 (미국 경제 지표 발표 타이밍 최적화)
- Google Vertex AI CustomJob 통합 (Fine-tuning 모드로 83% 성능 개선)
- 자동 매수 시간 변경: 23:50 → 00:30 (시장 개장 1시간 후, 변동성 안정화)
- 전체 스케줄 재구성: 데이터 수집 → 예측 → 검증 → 매수 → 재검증 플로우
