# 점수 모델 중앙화 구현 Plan — PR 1: 현행 행동 보존형 중앙화 (SSoT)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 6개 파일에 흩어진 점수 산식을 `ScoringPolicy` VO + monorepo 루트 `scoring_spec.yaml` 으로 통합. **산식 결과 변경 없음** (prod regression delta 0 목표). 사용자 노출 동작 동일하나 향후 PR 들의 baseline 제공.

**Architecture:** 5단계 PR 중 1단계. `scoring_spec.yaml` 이 SSoT. `ScoringPolicy` 가 spec load (env 경로 우선 + Docker 이미지 포함) + invariant 검증 + public API 노출. 4 호출처가 위임. **행동 변경 / 저장 계약 / VIX gate / disagreement 등은 별도 PR (§2 PR 분리).**

**Tech Stack:** Python 3.11 / Poetry / Pydantic Settings / Decimal (ROUND_HALF_UP 명시) / pytest / Hypothesis / PyYAML safe_load / 한글 commit messages

---

## 0. 설계 원칙

본 plan 은 다음 12 가지 원칙을 따른다:

| # | 원칙 | 적용 |
|---|------|------|
| **C1** | `scoring_spec.yaml` 운영 이미지 포함 보장 | `SCORING_SPEC_PATH` env 우선 + Docker COPY + startup validation + lru_cache (Task 1.1, 1.5) |
| **C2** | raw % vs normalized 0~1 입력 명확 분리 | Public API 2분리: `score_from_raw_signals()` (rise_pct % 단위) vs `compose_components()` (이미 normalized). 호출처 명시 변환 (Task 1.3) |
| **C3** | sentiment redistribute 등 행동 변경 분리 | **본 PR 범위 외**. PR 4 로 분리. 현행 sentiment N/A → 0점 보존 |
| **C4** | PostgreSQL/Kotlin 저장 계약 분리 | **본 PR 범위 외**. PR 2 로 분리. `Score` 객체 새 필드는 in-memory 만 |
| **H5** | 통합 테스트 — mongo fixture 기반 `_merge_sources` 관통 | Task 3.2 |
| **H6** | BuyCriteria 가 ScoringPolicy public API 만 호출 (private 금지) | Task 1.3 |
| **H7** | `Score` immutable — tuple + veto_reasons / warnings 분리 | Task 1.2 |
| **H8** | VIX gate handlers.py 위치 | **본 PR 범위 외**. PR 5 로 분리 |
| **M9** | PyYAML/Hypothesis 의존성 명시 | Task 1.0 |
| **M10** | Decimal rounding 명시 (`ROUND_HALF_UP`) | 모든 `quantize` (Task 1.3) |
| **M11** | Regression 스크립트 read-only 계정 + 사람 직접 실행 | Task 3.5, 4.2 |
| **M12** | Regression `$in` bulk fetch (N+3 회피) | Task 3.5 |

---

## 1. PR 분리 전체 로드맵 (참고)

| PR | 범위 | 본 plan |
|----|------|---------|
| **PR 1** | 현행 행동 보존형 중앙화 (SSoT only) | ✅ 본 plan |
| PR 2 | PostgreSQL `prediction_results` 컬럼 추가 + Kotlin Core 표시 계약 | 별도 plan |
| PR 3 | AI disagreement + negative AI veto (행동 변경) | 별도 plan |
| PR 4 | sentiment missing 재분배 (ADR 후 결정) | 별도 plan |
| PR 5 | VIX 거시 gate (handlers.py 진입) | 별도 plan |

각 PR 머지 후 regression 검증 → 다음 PR.

---

## 2. PR 1 Scope

### 포함
- `/quant-jump-stock/scoring_spec.yaml` 신설 (현행 가중치/임계/cap 값 100% 일치)
- `ScoringPolicy` VO (public API only, raw % vs component 분리)
- `Score` immutable dataclass (tuple, veto_reasons 분리)
- 4 호출처 (sync_service, buy_criteria, slack_notifier, technical_analysis) public API 위임
- Slack 분모 `composite_max` 동적 (현행 `7.4` 결과는 동일)
- Docker 이미지 spec 포함 + startup validation
- 통합 테스트 (mongo fixture) + golden + property + prod regression

### 명시적 제외 (별도 PR)
- AI source disagreement detection 적용 (PR 3)
- Negative AI veto (PR 3)
- Sentiment missing 재분배 (PR 4)
- VIX gate (PR 5)
- PostgreSQL/Kotlin 저장 계약 (PR 2)
- `technical_analysis.recommendation_score` dead code 제거 — 행동 변경이라 별도 PR (deprecate 마킹만)

### 성공 기준
- ✅ Unit + 통합 테스트 통과
- ✅ Prod 7일 regression: **의도된 변화 0건** (delta > 0.01 인 종목 0건)
- ✅ Slack 메시지 출력 글자 단위 동일 (분모 7.4 그대로)
- ✅ Docker 이미지 빌드 + Cloud Run startup 시 spec load 성공

---

## File Structure

### 신규
- `/Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock/scoring_spec.yaml` — SSoT
- `quant-jump-stock-data-engine/src/domain/recommendation/scoring_policy.py` — VO + spec loader
- `quant-jump-stock-data-engine/src/domain/recommendation/score.py` — frozen dataclass
- `quant-jump-stock-data-engine/tests/domain/recommendation/test_scoring_policy.py`
- `quant-jump-stock-data-engine/tests/domain/recommendation/test_score.py`
- `quant-jump-stock-data-engine/tests/domain/recommendation/test_property.py`
- `quant-jump-stock-data-engine/tests/domain/recommendation/test_golden.py`
- `quant-jump-stock-data-engine/tests/data/golden_v1_0_0.csv`
- `quant-jump-stock-data-engine/tests/application/recommendation/test_sync_integration.py`
- `quant-jump-stock-data-engine/scripts/scoring_regression_prod.py`

### 수정
- `quant-jump-stock-data-engine/src/application/recommendation/sync_service.py` — public API 위임
- `quant-jump-stock-data-engine/src/services/buy_criteria.py` — public API 위임
- `quant-jump-stock-data-engine/src/services/slack_notifier.py` — composite_max 동적 분모
- `quant-jump-stock-data-engine/src/services/technical_analysis.py` — *변경 없음* (dead code 제거는 별도 PR)
- `quant-jump-stock-data-engine/src/domain/recommendation/composite_grade.py` — `ScoreScale` 미사용 marker (실제 제거는 PR 2)
- `quant-jump-stock-data-engine/Dockerfile` — spec 파일 COPY
- `quant-jump-stock-data-engine/pyproject.toml` — PyYAML / Hypothesis 의존성

---

## Phase 1 — Foundation (Spec + VO + Docker)

### Task 1.0: 작업 브랜치 + 의존성

**Files:** `pyproject.toml`

- [ ] **Step 1: 작업 브랜치**

```bash
cd /Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock/quant-jump-stock-backend
git checkout main && git pull origin main
git checkout -b refactor/scoring-policy-ssot-pr1
```

- [ ] **Step 2: PyYAML / Hypothesis 의존성 추가**

```bash
cd quant-jump-stock-data-engine
poetry add 'pyyaml>=6.0.1'
poetry add --group dev 'hypothesis>=6'
poetry install
```

Verify:
```bash
poetry show pyyaml hypothesis 2>&1 | head -10
```

Expected: 둘 다 설치됨.

- [ ] **Step 3: Commit**

```bash
git add pyproject.toml poetry.lock
git commit -m "chore: PyYAML + Hypothesis 의존성 추가 (점수 모델 중앙화 준비)"
```

---

### Task 1.1: scoring_spec.yaml — 현행 값 100% 일치

**Files:** `/Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock/scoring_spec.yaml`

- [ ] **Step 1: Spec 파일 작성**

`/Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock/scoring_spec.yaml`:

```yaml
# 점수 모델 SSoT — PR 1 (현행 행동 보존형 중앙화)
# 본 파일은 monorepo 루트. Data Engine 이미지 빌드 시 COPY 됨.
# Python ScoringPolicy 가 SCORING_SPEC_PATH env 또는 default 경로로 로드.
# 본 PR 에서는 현행 가중치/임계/cap 값과 100% 일치 (regression 0 변화 목표).
# 행동 변경 (disagreement/veto/redistribute/VIX) 은 PR 3/4/5 에서 적용.

spec_version: "1.0.0"
formula_version: "1.0.0"  # 본 PR 은 현행 보존 → 1.0.0. 행동 변경 시 1.1.0+
description: |
  AI 예측 + 뉴스 감정 + 기술 분석 가중 합산. 현행 산식 100% 일치 보존.
  본 spec 변경 시 quant-jump-stock-data-engine 테스트 (golden + property +
  regression) 재실행 필수.

consumers:
  - python_data_engine
# PR 2 이후 kotlin_core_api 추가 — contract 변경 PR 강제

axes:
  ai:
    weight: 0.3
    max: 10.0
    cap_strategy: fixed_pct
    cap_threshold_pct: 20.0       # 현행 유지 (vol_normalized 는 PR 후속)
    missing_policy: zero           # 현행 유지
    negative_policy: zero          # 현행 유지 (PR 3 에서 veto 로 변경)
  technical:
    weight: 0.4
    max: 3.5
    components:
      golden_cross: 1.5
      rsi_below_threshold: 1.0
      macd_buy_signal: 1.0
    rsi_threshold: 70
    missing_policy: zero
  sentiment:
    weight: 0.3
    max: 10.0
    missing_policy: zero           # 현행 유지 (PR 4 에서 redistribute 검토)

composite_max: 7.4                 # derived: 0.3*10 + 0.4*3.5 + 0.3*10. load 시 검증

grades:
  S: { min: 6.0, label: "매우 강한 매수", color: emerald }
  A: { min: 4.5, label: "강한 매수",     color: cyan }
  B: { min: 3.0, label: "보통 매수",     color: yellow }
  C: { min: 1.5, label: "약한 매수",     color: orange }
  D: { min: 0.0, label: "신호 없음",     color: red }

# Slack 표시 라벨 — 본 PR 은 현행 RecommendationGrade.from_scores() 매핑 100% 보존
# 즉, confidence + tech_signals 기반 임계. PR 2 에서 grade 기반으로 변경 예정.
recommendation_labels:
  STRONG:    { min_confidence: 0.65, min_tech_signals: 3, label: "강력 추천", emoji: "🟢" }
  RECOMMEND: { min_confidence: 0.45, min_tech_signals: 2, label: "추천",       emoji: "🟡" }
  WATCH:     { min_confidence: 0.30, min_tech_signals: 1, label: "관심 종목",  emoji: "🟠" }
  NONE:      { label: "추천 없음", emoji: "⚪" }

recommendation_filter:
  min_composite_score: 2.0          # 현행 유지 (PR 3 에서 3.0 검토)
  max_recommendations: 5
```

- [ ] **Step 2: YAML 문법 + invariant 검증**

```bash
cd /Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock
python -c "
import yaml
s = yaml.safe_load(open('scoring_spec.yaml'))
ax = s['axes']
ws = sum(ax[k]['weight'] for k in ax)
assert abs(ws - 1.0) < 1e-9, f'weights sum {ws}'
cmax = sum(ax[k]['weight']*ax[k]['max'] for k in ax)
assert abs(cmax - s['composite_max']) < 1e-9, f'composite_max {cmax} vs {s[\"composite_max\"]}'
print(f'OK weights={ws} composite_max={cmax}')
"
```

Expected: `OK weights=1.0 composite_max=7.4`.

- [ ] **Step 3: Commit**

```bash
cd /Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock
git add scoring_spec.yaml
git commit -m "feat: 점수 모델 SSoT spec 파일 (PR 1, 현행 값 100% 일치)"
```

---

### Task 1.2: Score immutable dataclass (tuple + veto_reasons)

**Files:**
- Create: `quant-jump-stock-data-engine/src/domain/recommendation/score.py`
- Test: `quant-jump-stock-data-engine/tests/domain/recommendation/test_score.py`

- [ ] **Step 1: 디렉토리 + 실패 테스트**

```bash
cd quant-jump-stock-data-engine
mkdir -p tests/domain/recommendation tests/application/recommendation tests/services tests/data
touch tests/__init__.py tests/domain/__init__.py tests/domain/recommendation/__init__.py \
      tests/application/__init__.py tests/application/recommendation/__init__.py \
      tests/services/__init__.py
```

`tests/domain/recommendation/test_score.py`:

```python
"""Score frozen dataclass 검증 — 불변성 + tuple 필드."""
from decimal import Decimal
import pytest
from domain.recommendation.score import Score


def _make() -> Score:
    return Score(
        ai_score=Decimal("3.0"),
        tech_score=Decimal("3.5"),
        sentiment_score=Decimal("5.0"),
        composite_score=Decimal("5.90"),
        composite_max=Decimal("7.40"),
        confidence=Decimal("0.797"),
        grade="A",
        recommendation_label="RECOMMEND",
        missing_axes=(),
        veto_reasons=(),
        warnings=(),
    )


def test_score_is_frozen():
    s = _make()
    with pytest.raises((AttributeError, dataclass_error_type := type(None).__class__)):
        s.composite_score = Decimal("0")  # type: ignore


def test_score_missing_axes_is_tuple():
    s = _make()
    assert isinstance(s.missing_axes, tuple)
    assert isinstance(s.veto_reasons, tuple)
    assert isinstance(s.warnings, tuple)


def test_score_separates_missing_and_veto():
    """data missing 과 정책상 veto 는 별도 필드."""
    s = Score(
        ai_score=Decimal("0"),
        tech_score=Decimal("3.5"),
        sentiment_score=Decimal("0"),
        composite_score=Decimal("1.40"),
        composite_max=Decimal("7.40"),
        confidence=Decimal("0.189"),
        grade="D",
        recommendation_label="NONE",
        missing_axes=("sentiment",),       # 데이터 없음
        veto_reasons=(),                    # PR 3 에서 ai_negative_prediction 등 추가
        warnings=(),
    )
    assert "sentiment" in s.missing_axes
    assert s.veto_reasons == ()
```

- [ ] **Step 2: Test 실행 (실패 확인)**

```bash
poetry run pytest tests/domain/recommendation/test_score.py -v
```

Expected: `ImportError`.

- [ ] **Step 3: Score 구현**

`src/domain/recommendation/score.py`:

```python
"""Score — 점수 계산 결과 (frozen, immutable).

ScoringPolicy 의 점수 계산 메서드들이 반환하는 값 객체.
모든 collection 필드는 tuple 로 강제하여 변경 불가.

필드 의미 (리뷰 #7 반영):
  - missing_axes: 데이터 부재 축 ("ai" / "tech" / "sentiment")
  - veto_reasons: 정책상 추천 제외 사유 (PR 3 에서 "ai_negative" 등 추가)
  - warnings: 사용자 노출용 경고 (PR 5 에서 VIX 등)
"""
from dataclasses import dataclass, field
from decimal import Decimal
from typing import Tuple


@dataclass(frozen=True, slots=True)
class Score:
    ai_score: Decimal
    tech_score: Decimal
    sentiment_score: Decimal
    composite_score: Decimal
    composite_max: Decimal
    confidence: Decimal
    grade: str                       # "S" / "A" / "B" / "C" / "D"
    recommendation_label: str        # "STRONG" / "RECOMMEND" / "WATCH" / "NONE"
    missing_axes: Tuple[str, ...] = field(default=())
    veto_reasons: Tuple[str, ...] = field(default=())
    warnings: Tuple[str, ...] = field(default=())
```

- [ ] **Step 4: Test 통과**

```bash
poetry run pytest tests/domain/recommendation/test_score.py -v
```

Expected: `3 passed`.

- [ ] **Step 5: Commit**

```bash
git add quant-jump-stock-data-engine/src/domain/recommendation/score.py \
        quant-jump-stock-data-engine/tests/__init__.py \
        quant-jump-stock-data-engine/tests/domain/__init__.py \
        quant-jump-stock-data-engine/tests/domain/recommendation/__init__.py \
        quant-jump-stock-data-engine/tests/domain/recommendation/test_score.py
git commit -m "feat(scoring): Score frozen dataclass — tuple + veto_reasons 분리"
```

---

### Task 1.3: ScoringPolicy — Public API (raw vs component 분리) + Spec loader

**Files:**
- Create: `src/domain/recommendation/scoring_policy.py`
- Create: `src/domain/recommendation/exceptions.py`
- Test: `tests/domain/recommendation/test_scoring_policy.py`

리뷰 #1, #2, #6, #10 반영:
- `SCORING_SPEC_PATH` env 우선
- `lru_cache` 싱글톤
- Public API 2분리: raw % vs component
- BuyCriteria 호출용 public 메서드 추가 (`tech_score_from_indicators`, `compose_components`)
- 모든 quantize 에 `rounding=ROUND_HALF_UP` 명시

- [ ] **Step 1: Exception 클래스 + 실패 테스트**

`src/domain/recommendation/exceptions.py`:

```python
class SpecValidationError(ValueError):
    """scoring_spec.yaml 형식/invariant 위반."""


class SpecNotFoundError(FileNotFoundError):
    """SCORING_SPEC_PATH 또는 default 경로에서 spec 미발견."""
```

`tests/domain/recommendation/test_scoring_policy.py`:

```python
"""ScoringPolicy — spec load + invariant + public API."""
import os
from decimal import Decimal
from pathlib import Path
import pytest
from domain.recommendation.scoring_policy import ScoringPolicy
from domain.recommendation.exceptions import SpecValidationError, SpecNotFoundError


# ── Spec load 경로 ──────────────────────────────────────

def test_load_default_finds_monorepo_root_spec():
    """본 plan default 경로 — 모노레포 루트 scoring_spec.yaml."""
    p = ScoringPolicy.load_default()
    assert p.formula_version == "1.0.0"
    assert p.composite_max == Decimal("7.4")


def test_load_via_env_path(tmp_path, monkeypatch):
    """SCORING_SPEC_PATH env 우선 (리뷰 #1)."""
    custom = tmp_path / "custom_spec.yaml"
    custom.write_text((Path(__file__).parents[3] / "../../../scoring_spec.yaml").read_text())
    monkeypatch.setenv("SCORING_SPEC_PATH", str(custom))
    ScoringPolicy._cached_default.cache_clear()  # noqa
    p = ScoringPolicy.load_default()
    assert p.formula_version == "1.0.0"


def test_load_missing_file_raises():
    with pytest.raises(SpecNotFoundError):
        ScoringPolicy.load("/nonexistent/path.yaml")


# ── Invariant ────────────────────────────────────────────

def _write_spec(tmp_path, override):
    base = {
        "spec_version": "1.0.0",
        "formula_version": "1.0.0",
        "axes": {
            "ai":        {"weight": 0.3, "max": 10.0, "cap_strategy": "fixed_pct",
                          "cap_threshold_pct": 20.0, "missing_policy": "zero",
                          "negative_policy": "zero"},
            "technical": {"weight": 0.4, "max": 3.5,
                          "components": {"golden_cross": 1.5, "rsi_below_threshold": 1.0,
                                         "macd_buy_signal": 1.0},
                          "rsi_threshold": 70, "missing_policy": "zero"},
            "sentiment": {"weight": 0.3, "max": 10.0, "missing_policy": "zero"},
        },
        "composite_max": 7.4,
        "grades": {"S": {"min": 6.0, "label": "S"}, "A": {"min": 4.5, "label": "A"},
                   "B": {"min": 3.0, "label": "B"}, "C": {"min": 1.5, "label": "C"},
                   "D": {"min": 0.0, "label": "D"}},
        "recommendation_labels": {"NONE": {"label": "x", "emoji": "x"}},
    }
    # deep merge override
    for k, v in (override or {}).items():
        if isinstance(v, dict) and isinstance(base.get(k), dict):
            base[k].update(v)
        else:
            base[k] = v
    import yaml
    p = tmp_path / "bad.yaml"
    p.write_text(yaml.dump(base))
    return p


def test_invariant_weights_must_sum_to_one(tmp_path):
    bad = _write_spec(tmp_path, {"axes": {"ai": {"weight": 0.5, "max": 10.0,
                                                 "cap_strategy": "fixed_pct",
                                                 "cap_threshold_pct": 20.0,
                                                 "missing_policy": "zero",
                                                 "negative_policy": "zero"}}})
    with pytest.raises(SpecValidationError, match="weights sum"):
        ScoringPolicy.load(str(bad))


def test_invariant_composite_max_matches_derived(tmp_path):
    bad = _write_spec(tmp_path, {"composite_max": 99.9})
    with pytest.raises(SpecValidationError, match="composite_max mismatch"):
        ScoringPolicy.load(str(bad))


def test_invariant_grade_thresholds_monotonic(tmp_path):
    bad = _write_spec(tmp_path, {"grades": {
        "S": {"min": 4.0, "label": "S"},
        "A": {"min": 5.0, "label": "A"},  # 단조성 위반
        "B": {"min": 3.0, "label": "B"},
        "C": {"min": 1.5, "label": "C"},
        "D": {"min": 0.0, "label": "D"},
    }})
    with pytest.raises(SpecValidationError, match="grade thresholds"):
        ScoringPolicy.load(str(bad))
```

- [ ] **Step 2: Test 실행 (실패 확인)**

```bash
poetry run pytest tests/domain/recommendation/test_scoring_policy.py -v
```

Expected: `ImportError`.

- [ ] **Step 3: `ScoringPolicy` 구현 — spec loader + invariant**

`src/domain/recommendation/scoring_policy.py`:

```python
"""ScoringPolicy — 점수 산식 단일 진실원 (SSoT, PR 1).

리뷰 반영:
  - SCORING_SPEC_PATH env 우선 (C1)
  - raw % vs component score Public API 분리 (C2)
  - private method 호출 금지 — public API 만 노출 (H6)
  - ROUND_HALF_UP quantize 명시 (M10)
"""
from __future__ import annotations

import os
from decimal import Decimal, ROUND_HALF_UP
from functools import lru_cache
from pathlib import Path
from typing import Optional, Dict, Any, Tuple

import yaml

from domain.recommendation.exceptions import SpecValidationError, SpecNotFoundError
from domain.recommendation.score import Score


# 기본 경로: monorepo 루트의 scoring_spec.yaml
# src/domain/recommendation -> src/domain -> src -> data-engine -> backend -> monorepo
_FALLBACK_DEFAULT = Path(__file__).resolve().parents[5] / "scoring_spec.yaml"
_QTZ_2 = Decimal("0.01")


class ScoringPolicy:
    """spec 파일로부터 로드된 점수 정책 (immutable after load)."""

    def __init__(self, spec: Dict[str, Any]):
        self._spec = spec
        self._validate_invariants(spec)

    # ── factory ─────────────────────────────────────────
    @classmethod
    def load(cls, path: str) -> "ScoringPolicy":
        p = Path(path)
        if not p.exists():
            raise SpecNotFoundError(f"scoring spec not found: {path}")
        with open(p, "r", encoding="utf-8") as f:
            spec = yaml.safe_load(f)
        return cls(spec)

    @classmethod
    @lru_cache(maxsize=1)
    def _cached_default(cls) -> "ScoringPolicy":
        path = os.environ.get("SCORING_SPEC_PATH") or str(_FALLBACK_DEFAULT)
        return cls.load(path)

    @classmethod
    def load_default(cls) -> "ScoringPolicy":
        """Cached default (SCORING_SPEC_PATH 우선 → fallback 경로)."""
        return cls._cached_default()

    # ── invariant 검증 ─────────────────────────────────
    @staticmethod
    def _validate_invariants(spec: Dict[str, Any]) -> None:
        axes = spec.get("axes") or {}
        if set(axes.keys()) != {"ai", "technical", "sentiment"}:
            raise SpecValidationError(f"axes keys must be {{ai, technical, sentiment}}; got {set(axes.keys())}")

        w_sum = sum(Decimal(str(axes[k]["weight"])) for k in axes)
        if abs(w_sum - Decimal("1.0")) > Decimal("0.001"):
            raise SpecValidationError(f"axes weights sum must be 1.0; got {w_sum}")

        derived = sum(Decimal(str(axes[k]["weight"])) * Decimal(str(axes[k]["max"])) for k in axes)
        declared = Decimal(str(spec.get("composite_max", 0)))
        if abs(derived - declared) > Decimal("0.001"):
            raise SpecValidationError(
                f"composite_max mismatch: declared {declared} vs derived {derived}"
            )

        grades = spec.get("grades") or {}
        order = ["S", "A", "B", "C", "D"]
        thresholds = [Decimal(str(grades[g]["min"])) for g in order if g in grades]
        if thresholds != sorted(thresholds, reverse=True):
            raise SpecValidationError(f"grade thresholds must be monotonic decreasing; got {thresholds}")

    # ── 속성 ─────────────────────────────────────────
    @property
    def spec(self) -> Dict[str, Any]:
        return self._spec

    @property
    def formula_version(self) -> str:
        return self._spec["formula_version"]

    @property
    def composite_max(self) -> Decimal:
        return Decimal(str(self._spec["composite_max"]))

    @property
    def axes(self) -> Dict[str, Dict[str, Any]]:
        return self._spec["axes"]

    @property
    def rsi_threshold(self) -> Decimal:
        return Decimal(str(self._spec["axes"]["technical"]["rsi_threshold"]))

    @property
    def ai_cap_pct(self) -> Decimal:
        return Decimal(str(self._spec["axes"]["ai"]["cap_threshold_pct"]))

    @property
    def min_composite_score(self) -> Decimal:
        return Decimal(str(self._spec["recommendation_filter"]["min_composite_score"]))

    # ── Public: raw % → AI 점수 변환 (리뷰 C2) ──────────
    def normalize_rise_pct_to_score(self, rise_pct: Optional[Decimal]) -> Decimal:
        """raw rise percentage (%, 예: 20.0) → AI score (0~max_ai).

        현행 산식 100% 보존:
          normalized = clip(0.5 + rise_pct / (2 * cap), 0, 1)
          if normalized < 0.5 → 0  (음수 예측은 0점. PR 3 에서 veto 로 변경)
          else → (normalized - 0.5) * 2 * max_ai
        """
        if rise_pct is None:
            return Decimal("0")
        cap = self.ai_cap_pct
        normalized = Decimal("0.5") + rise_pct / (Decimal("2") * cap)
        normalized = max(Decimal("0"), min(Decimal("1"), normalized))
        if normalized < Decimal("0.5"):
            return Decimal("0")
        ai_max = Decimal(str(self.axes["ai"]["max"]))
        v = (normalized - Decimal("0.5")) * Decimal("2") * ai_max
        return v.quantize(_QTZ_2, rounding=ROUND_HALF_UP)

    # ── Public: tech indicators → tech score ─────────────
    def tech_score_from_indicators(self, indicators: Optional[Dict[str, Any]]) -> Decimal:
        if not indicators:
            return Decimal("0")
        cfg = self.axes["technical"]
        comps = cfg["components"]
        s = Decimal("0")
        if indicators.get("golden_cross"):
            s += Decimal(str(comps["golden_cross"]))
        rsi = indicators.get("rsi")
        if rsi is not None and Decimal(str(rsi)) < self.rsi_threshold:
            s += Decimal(str(comps["rsi_below_threshold"]))
        if indicators.get("macd_buy_signal"):
            s += Decimal(str(comps["macd_buy_signal"]))
        return s

    def count_tech_signals(self, indicators: Optional[Dict[str, Any]]) -> int:
        if not indicators:
            return 0
        cnt = 0
        if indicators.get("golden_cross"):
            cnt += 1
        rsi = indicators.get("rsi")
        if rsi is not None and Decimal(str(rsi)) < self.rsi_threshold:
            cnt += 1
        if indicators.get("macd_buy_signal"):
            cnt += 1
        return cnt

    # ── Public: sentiment (-1~+1) → sentiment score ─────
    def sentiment_score_from_raw(self, sentiment: Optional[Decimal]) -> Decimal:
        if sentiment is None:
            return Decimal("0")
        v = Decimal(str(sentiment))
        if v <= Decimal("0"):
            return Decimal("0")
        sent_max = Decimal(str(self.axes["sentiment"]["max"]))
        return (v * sent_max).quantize(_QTZ_2, rounding=ROUND_HALF_UP)

    # ── Public: compose components → Score ──────────────
    def compose_components(
        self,
        ai_score: Decimal,
        tech_score: Decimal,
        sentiment_score: Decimal,
        has_ai: bool,
        has_tech: bool,
        has_sentiment: bool,
        tech_signal_count: int,
    ) -> Score:
        """이미 normalized 된 component score 들로부터 Score 계산.

        본 PR 은 현행 buy_criteria 동작 보존: missing 시 0점 처리 (재분배 없음).
        """
        ax = self.axes
        w_ai = Decimal(str(ax["ai"]["weight"]))
        w_tech = Decimal(str(ax["technical"]["weight"]))
        w_sent = Decimal(str(ax["sentiment"]["weight"]))

        effective_ai = ai_score if has_ai else Decimal("0")
        effective_tech = tech_score if has_tech else Decimal("0")
        effective_sent = sentiment_score if has_sentiment else Decimal("0")

        composite = (w_ai * effective_ai + w_tech * effective_tech + w_sent * effective_sent)
        composite_max = self.composite_max
        confidence = (composite / composite_max).quantize(Decimal("0.001"), rounding=ROUND_HALF_UP) \
            if composite_max > 0 else Decimal("0")

        grade = self.grade_for_composite(composite)
        label = self._label_from_confidence_and_signals(confidence, tech_signal_count)

        missing = tuple(
            name for name, present in
            [("ai", has_ai), ("tech", has_tech), ("sentiment", has_sentiment)]
            if not present
        )

        return Score(
            ai_score=effective_ai.quantize(_QTZ_2, rounding=ROUND_HALF_UP),
            tech_score=effective_tech.quantize(_QTZ_2, rounding=ROUND_HALF_UP),
            sentiment_score=effective_sent.quantize(_QTZ_2, rounding=ROUND_HALF_UP),
            composite_score=composite.quantize(_QTZ_2, rounding=ROUND_HALF_UP),
            composite_max=composite_max.quantize(_QTZ_2, rounding=ROUND_HALF_UP),
            confidence=confidence,
            grade=grade,
            recommendation_label=label,
            missing_axes=missing,
            veto_reasons=(),  # PR 3 에서 채워짐
            warnings=(),       # PR 5 에서 VIX 경고
        )

    # ── Public: raw signals → Score (one-shot) ──────────
    def score_from_raw_signals(
        self,
        rise_pct: Optional[Decimal],
        sentiment_raw: Optional[Decimal],
        tech_indicators: Optional[Dict[str, Any]],
    ) -> Score:
        """raw input (rise % / sentiment -1~+1 / tech indicators) → Score 단일 호출."""
        ai_s = self.normalize_rise_pct_to_score(rise_pct)
        tech_s = self.tech_score_from_indicators(tech_indicators)
        sent_s = self.sentiment_score_from_raw(sentiment_raw)
        return self.compose_components(
            ai_score=ai_s,
            tech_score=tech_s,
            sentiment_score=sent_s,
            has_ai=rise_pct is not None,
            has_tech=bool(tech_indicators),
            has_sentiment=sentiment_raw is not None,
            tech_signal_count=self.count_tech_signals(tech_indicators),
        )

    # ── Grade / Label ───────────────────────────────────
    def grade_for_composite(self, composite: Decimal) -> str:
        for g in ["S", "A", "B", "C"]:
            if composite >= Decimal(str(self._spec["grades"][g]["min"])):
                return g
        return "D"

    def _label_from_confidence_and_signals(self, confidence: Decimal, tech_signals: int) -> str:
        """본 PR 은 현행 RecommendationGrade.from_scores() 매핑 100% 보존."""
        labels = self._spec["recommendation_labels"]
        for key in ["STRONG", "RECOMMEND", "WATCH"]:
            cfg = labels.get(key) or {}
            min_c = Decimal(str(cfg.get("min_confidence", 0)))
            min_s = cfg.get("min_tech_signals", 0)
            if confidence >= min_c and tech_signals >= min_s:
                return key
        return "NONE"

    def label_metadata(self, label_key: str) -> Dict[str, str]:
        cfg = self._spec["recommendation_labels"].get(label_key) or {}
        return {"label": cfg.get("label", ""), "emoji": cfg.get("emoji", "⚪")}
```

- [ ] **Step 4: Test 통과**

```bash
poetry run pytest tests/domain/recommendation/test_scoring_policy.py -v
```

Expected: `6 passed`.

- [ ] **Step 5: Commit**

```bash
git add quant-jump-stock-data-engine/src/domain/recommendation/scoring_policy.py \
        quant-jump-stock-data-engine/src/domain/recommendation/exceptions.py \
        quant-jump-stock-data-engine/tests/domain/recommendation/test_scoring_policy.py
git commit -m "feat(scoring): ScoringPolicy public API + invariant + env path 지원"
```

---

### Task 1.4: ScoringPolicy.compose_components 단위 테스트 — 현행 동작 검증

**Files:**
- Test: `tests/domain/recommendation/test_scoring_policy.py` (확장)

- [ ] **Step 1: 실패 테스트 — 산식 결과 현행과 일치**

`test_scoring_policy.py` 에 추가:

```python
def test_score_all_signals_strong_matches_current():
    """현행 sync_service 결과와 동일: AI=10, Tech=3.5, Sent=5 → composite 5.9 / grade A."""
    p = ScoringPolicy.load_default()
    s = p.compose_components(
        ai_score=Decimal("10"),
        tech_score=Decimal("3.5"),
        sentiment_score=Decimal("5"),
        has_ai=True,
        has_tech=True,
        has_sentiment=True,
        tech_signal_count=3,
    )
    assert s.composite_score == Decimal("5.90")
    assert s.composite_max == Decimal("7.40")
    assert s.confidence == Decimal("0.797")
    assert s.grade == "A"
    assert s.recommendation_label == "STRONG"  # confidence 0.797 >= 0.65, signals 3 >= 3
    assert s.missing_axes == ()


def test_score_sentiment_missing_zero_policy_preserved():
    """본 PR: sentiment N/A → 0점 처리 보존 (PR 4 에서 재분배 적용)."""
    p = ScoringPolicy.load_default()
    s = p.compose_components(
        ai_score=Decimal("10"),
        tech_score=Decimal("3.5"),
        sentiment_score=Decimal("0"),
        has_ai=True,
        has_tech=True,
        has_sentiment=False,         # 누락
        tech_signal_count=3,
    )
    # 현행: 0.3*10 + 0.4*3.5 + 0.3*0 = 4.4. composite_max 는 7.4 유지 (재분배 없음)
    assert s.composite_score == Decimal("4.40")
    assert s.composite_max == Decimal("7.40")
    assert "sentiment" in s.missing_axes


def test_score_raw_rise_pct_20_pct_full_score():
    """raw +20% → AI score 10 (현행 cap 동작)."""
    p = ScoringPolicy.load_default()
    score = p.score_from_raw_signals(
        rise_pct=Decimal("20"),
        sentiment_raw=Decimal("0.5"),
        tech_indicators={"golden_cross": True, "rsi": 50, "macd_buy_signal": True},
    )
    # AI=10, Tech=3.5, Sent=5 → composite 5.9
    assert score.composite_score == Decimal("5.90")


def test_score_negative_rise_pct_zero_policy_preserved():
    """본 PR: -15% → AI score 0 (veto 없이 0점). PR 3 에서 veto 로 변경."""
    p = ScoringPolicy.load_default()
    score = p.score_from_raw_signals(
        rise_pct=Decimal("-15"),
        sentiment_raw=Decimal("0.5"),
        tech_indicators={"golden_cross": True, "rsi": 50, "macd_buy_signal": True},
    )
    # AI=0, Tech=3.5, Sent=5 → composite 0 + 1.4 + 1.5 = 2.9
    assert score.composite_score == Decimal("2.90")
    assert score.veto_reasons == ()  # 본 PR 은 veto 미적용
```

- [ ] **Step 2: Test 통과 (모두 정확한 현행 값과 일치)**

```bash
poetry run pytest tests/domain/recommendation/test_scoring_policy.py -v
```

Expected: 모두 통과.

- [ ] **Step 3: Commit**

```bash
git add quant-jump-stock-data-engine/tests/domain/recommendation/test_scoring_policy.py
git commit -m "test(scoring): ScoringPolicy 현행 동작 보존 검증"
```

---

### Task 1.5: Docker 이미지 spec 포함 + Startup validation

**Files:**
- Modify: `quant-jump-stock-data-engine/Dockerfile`
- Modify: `quant-jump-stock-data-engine/src/main.py` (startup hook)

리뷰 #1 핵심 반영.

- [ ] **Step 1: Dockerfile 현황 확인**

```bash
cat /Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock/quant-jump-stock-backend/quant-jump-stock-data-engine/Dockerfile | head -30
```

- [ ] **Step 2: Dockerfile 수정 — monorepo 루트 spec COPY**

Build context 가 `quant-jump-stock-data-engine/` 이라 monorepo 루트 spec 접근 불가. 두 가지 옵션:

**옵션 A (권장): build context 를 monorepo 루트로 변경**
- workflow `.github/workflows/deploy-data-engine.yml` 의 `context: ./quant-jump-stock-data-engine` → `context: .`
- Dockerfile 안에서 `COPY scoring_spec.yaml /app/scoring_spec.yaml`
- ENV `SCORING_SPEC_PATH=/app/scoring_spec.yaml` 설정

**옵션 B: Dockerfile 안에 spec 복사본 유지**
- 빌드 전 sync script 가 `scoring_spec.yaml` 을 data-engine 내부로 복사
- Drift 위험 → 비권장

옵션 A 채택. 작업:

`.github/workflows/deploy-data-engine.yml` 수정:
```yaml
- name: Build & Push Data Engine
  uses: docker/build-push-action@v6
  with:
    context: .                               # ← monorepo 루트로 변경
    file: ./quant-jump-stock-data-engine/Dockerfile  # ← Dockerfile 경로 명시
    push: true
    tags: ${{ env.AR_REPO }}/qjs-data-engine:latest
    ...
```

`Dockerfile` 추가:
```dockerfile
# 기존 COPY 라인들 근처에 추가
COPY scoring_spec.yaml /app/scoring_spec.yaml
ENV SCORING_SPEC_PATH=/app/scoring_spec.yaml
```

그리고 기존 `COPY ./quant-jump-stock-data-engine/` 같이 prefix 변경 필요할 수 있음 (build context 변경 영향). 변경 후 로컬 빌드로 검증.

- [ ] **Step 3: 로컬 docker build 검증**

```bash
cd /Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock
docker build \
  -t qjs-data-engine:test-pr1 \
  -f quant-jump-stock-backend/quant-jump-stock-data-engine/Dockerfile \
  .
```

Expected: build 성공.

- [ ] **Step 4: spec 파일 컨테이너 내 존재 검증**

```bash
docker run --rm qjs-data-engine:test-pr1 cat /app/scoring_spec.yaml | head -5
```

Expected: spec 내용 출력.

- [ ] **Step 5: Startup validation 추가** — `main.py` lifespan / 모듈 진입 시

```python
# main.py 상단 import 영역에 추가
from domain.recommendation.scoring_policy import ScoringPolicy

# FastAPI app 초기화 후 또는 lifespan handler 안에서
def _validate_scoring_spec_on_startup() -> None:
    """Cloud Run startup 시 scoring_spec 로드 + invariant 검증 (리뷰 C1).

    실패 시 즉시 fail-fast — Cloud Run 이 instance 를 재시작 또는 deployment fail.
    """
    try:
        p = ScoringPolicy.load_default()
        logger.info(
            f"✅ scoring_spec loaded: version={p.formula_version}, "
            f"composite_max={p.composite_max}, path={os.environ.get('SCORING_SPEC_PATH', 'default')}"
        )
    except Exception as e:
        logger.critical(f"❌ scoring_spec load failed at startup: {e}")
        raise  # Cloud Run 이 startup probe fail 로 인식


# app 초기화 직후 호출
_validate_scoring_spec_on_startup()
```

- [ ] **Step 6: 로컬 main.py 실행 — startup 로그 확인**

```bash
cd quant-jump-stock-data-engine
SCORING_SPEC_PATH=/Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock/scoring_spec.yaml \
  poetry run python -c "from src.main import _validate_scoring_spec_on_startup; _validate_scoring_spec_on_startup()" 2>&1 | head -5
```

Expected: `✅ scoring_spec loaded ...` 로그 출력.

- [ ] **Step 7: Commit**

```bash
git add quant-jump-stock-backend/quant-jump-stock-data-engine/Dockerfile \
        quant-jump-stock-backend/quant-jump-stock-data-engine/src/main.py \
        quant-jump-stock-backend/.github/workflows/deploy-data-engine.yml
git commit -m "feat(scoring): Docker 이미지에 scoring_spec.yaml 포함 + startup validation"
```

---

## Phase 2 — 호출처 위임 (Public API)

### Task 2.1: sync_service — 현행 동일 결과로 위임

**Files:**
- Modify: `src/application/recommendation/sync_service.py`
- Test: `tests/application/recommendation/test_sync_service_policy_parity.py`

리뷰 #2 (input scale 혼선) 핵심 반영. sync_service 가 어떤 입력을 가지고 있는지 정확히 추적 후 public API 매핑.

- [ ] **Step 1: 현재 sync_service 의 산식 입력 형식 분석**

```bash
grep -n "_fetch_ai_predictions\|_fetch_stock_analysis_results\|rise_probability\|rise_pct" \
  src/application/recommendation/sync_service.py | head -30
```

기록 (코드 reading 후 plan 에 명시):
- `_fetch_stock_analysis_results` 반환: `rise_probability` = **normalized 0~1** (line 134)
- `_fetch_ai_predictions` 반환: `rise_probability` = **normalized 0~1** (line 219)
- `_merge_ai_sources` 반환: `rise_probability` (normalized)
- 기존 `_calculate_ai_score` 시그니처: `(rise_probability_0_to_1) → 0~10` (normalized 입력)

→ 본 PR 에서 sync_service 는 **이미 normalized 된 값**을 가지고 있음. ScoringPolicy 의 `score_from_raw_signals(rise_pct=raw_%)` 가 아니라 **normalized 를 score 로 변환하는 추가 헬퍼** 필요. 또는 sync_service 가 raw % 를 보존하도록 fetch 단계에서 추가.

**본 PR 결정**: ScoringPolicy 에 `ai_score_from_normalized(prob: Decimal)` public 메서드 추가. sync_service 는 normalized 값을 그대로 넘김. **현행 결과 100% 일치 보장**.

- [ ] **Step 2: ScoringPolicy 에 `ai_score_from_normalized` 추가**

`scoring_policy.py` 에 추가:

```python
    # ── Public: normalized AI probability (0~1) → AI score ──────
    def ai_score_from_normalized(self, normalized: Optional[Decimal]) -> Decimal:
        """이미 normalized 된 rise_probability (0~1) → AI score (0~max_ai).

        sync_service 가 이미 normalized 한 값을 가지고 있는 경우 사용.
        raw rise_pct 가 있다면 normalize_rise_pct_to_score() 사용.
        """
        if normalized is None:
            return Decimal("0")
        v = Decimal(str(normalized))
        v = max(Decimal("0"), min(Decimal("1"), v))
        if v < Decimal("0.5"):
            return Decimal("0")
        ai_max = Decimal(str(self.axes["ai"]["max"]))
        return ((v - Decimal("0.5")) * Decimal("2") * ai_max).quantize(_QTZ_2, rounding=ROUND_HALF_UP)
```

테스트도 `test_scoring_policy.py` 에 추가:
```python
def test_ai_score_from_normalized_matches_current_sync_service():
    """sync_service 에서 normalized 1.0 → 10 (max)."""
    p = ScoringPolicy.load_default()
    assert p.ai_score_from_normalized(Decimal("1.0")) == Decimal("10.00")
    assert p.ai_score_from_normalized(Decimal("0.5")) == Decimal("0")
    assert p.ai_score_from_normalized(Decimal("0.75")) == Decimal("5.00")
    assert p.ai_score_from_normalized(None) == Decimal("0")
```

- [ ] **Step 3: Test 통과**

```bash
poetry run pytest tests/domain/recommendation/test_scoring_policy.py::test_ai_score_from_normalized_matches_current_sync_service -v
```

- [ ] **Step 4: 실패 테스트 — sync_service parity**

`tests/application/recommendation/test_sync_service_policy_parity.py`:

```python
"""sync_service composite 가 ScoringPolicy.compose_components 와 100% 일치."""
from decimal import Decimal
from domain.recommendation.scoring_policy import ScoringPolicy


def test_compose_parity_with_legacy_sync_formula():
    """현행 sync_service:
       composite = 0.3*ai_score + 0.4*tech_score + 0.3*sentiment_score
       (AI 10, Tech 3.5, Sent 10 scale)
    ScoringPolicy 가 동일한 결과를 내야 함."""
    p = ScoringPolicy.load_default()
    cases = [
        # (ai, tech, sent, expected_composite)
        (Decimal("10"), Decimal("3.5"), Decimal("5"), Decimal("5.90")),
        (Decimal("0"),  Decimal("3.5"), Decimal("0"), Decimal("1.40")),
        (Decimal("5"),  Decimal("0"),   Decimal("3"), Decimal("2.40")),
        (Decimal("10"), Decimal("3.5"), Decimal("10"), Decimal("7.40")),
    ]
    for ai, tech, sent, expected in cases:
        s = p.compose_components(
            ai_score=ai, tech_score=tech, sentiment_score=sent,
            has_ai=True, has_tech=True, has_sentiment=True,
            tech_signal_count=3,
        )
        assert s.composite_score == expected, f"({ai}, {tech}, {sent}) → {s.composite_score} != {expected}"
```

- [ ] **Step 5: Test 통과**

```bash
poetry run pytest tests/application/recommendation/test_sync_service_policy_parity.py -v
```

- [ ] **Step 6: sync_service 변경 — `_calculate_composite_score` / `_calculate_ai_score` / `_calculate_sentiment_score` / `_calculate_tech_score` 4개 메서드 → policy 위임**

`sync_service.py` 변경 (line 27 import + line 460-560 메서드 4개 제거 + `_merge_sources` 호출부 교체):

```python
# 상단 import 추가
from domain.recommendation.scoring_policy import ScoringPolicy
from domain.recommendation.score import Score

# __init__ 시그니처
class RecommendationSyncService:
    def __init__(
        self,
        mongo_db,
        pg_pool=None,
        policy: Optional[ScoringPolicy] = None,
    ):
        self.mongo_db = mongo_db
        self.pg_pool = pg_pool
        self._policy = policy or ScoringPolicy.load_default()
        # 기존 self.weight_ai / weight_tech / weight_sentiment / rsi_threshold 필드는
        # property 로 노출 (legacy 호환). 실제 산식은 policy 에 위임.

    @property
    def weight_ai(self) -> Decimal:
        return Decimal(str(self._policy.axes["ai"]["weight"]))

    @property
    def weight_tech(self) -> Decimal:
        return Decimal(str(self._policy.axes["technical"]["weight"]))

    @property
    def weight_sentiment(self) -> Decimal:
        return Decimal(str(self._policy.axes["sentiment"]["weight"]))

    @property
    def rsi_threshold(self) -> Decimal:
        return self._policy.rsi_threshold

# 메서드 4개 제거:
#   _calculate_ai_score, _calculate_sentiment_score,
#   _calculate_tech_score, _calculate_composite_score
# 대신 호출처 (_merge_sources) 에서 policy public API 직접 호출.
```

`_merge_sources` 의 점수 계산 블록 (대략 line 380-460):

```python
# 기존:
#   ai_score = self._calculate_ai_score(rise_probability)
#   sentiment_score = self._calculate_sentiment_score(sentiment.get("sentiment_score"))
#   tech_score = self._calculate_tech_score(tech) if tech else Decimal("0")
#   tech_signals_count = self._count_tech_signals(tech) if tech else 0
#   composite_score = self._calculate_composite_score(...)
#   grade = self._determine_grade(composite_score)
# 신:
ai_score = self._policy.ai_score_from_normalized(
    Decimal(str(rise_probability)) if rise_probability is not None else None
)
tech_score = self._policy.tech_score_from_indicators(tech) if tech else Decimal("0")
tech_signals_count = self._policy.count_tech_signals(tech) if tech else 0
sent_raw = sentiment.get("sentiment_score")
sentiment_score = self._policy.sentiment_score_from_raw(
    Decimal(str(sent_raw)) if sent_raw is not None else None
)

has_ai = rise_probability is not None
has_sentiment = sent_raw is not None
has_tech = bool(tech)

score_result = self._policy.compose_components(
    ai_score=ai_score, tech_score=tech_score, sentiment_score=sentiment_score,
    has_ai=has_ai, has_tech=has_tech, has_sentiment=has_sentiment,
    tech_signal_count=tech_signals_count,
)
composite_score = score_result.composite_score
grade = score_result.grade
```

merged dict 에 `score` 객체 추가 (slack 사용):
```python
merged.append({
    "ticker": ticker,
    "stock_name": stock_name,
    "score": score_result,   # 신규: Score 객체 통째
    # ... 기존 필드 그대로 유지 ...
    "ai_predicted_price": ai.get("predicted_price"),
    "ai_rise_probability": rise_probability,
    "ai_score": float(score_result.ai_score),
    "sentiment_score": sent_raw,
    "sentiment_normalized_score": float(score_result.sentiment_score),
    "tech_score": float(score_result.tech_score),
    "tech_signals_count": tech_signals_count,
    "composite_score": float(score_result.composite_score),
    "composite_grade": grade,
    "is_recommended": self._determine_recommended(score_result, price_recommendation),
    # ... 기존 필드 ...
})
```

- [ ] **Step 7: Test 통과 (sync_service 기존 + 신규)**

```bash
poetry run pytest tests/application/ tests/services/ -v 2>&1 | tail -20
```

Expected: 통과.

- [ ] **Step 8: Commit**

```bash
git add quant-jump-stock-data-engine/src/application/recommendation/sync_service.py \
        quant-jump-stock-data-engine/src/domain/recommendation/scoring_policy.py \
        quant-jump-stock-data-engine/tests/application/recommendation/test_sync_service_policy_parity.py \
        quant-jump-stock-data-engine/tests/domain/recommendation/test_scoring_policy.py
git commit -m "refactor(sync): composite 산식 ScoringPolicy public API 위임 (현행 동일)"
```

---

### Task 2.2: buy_criteria — Public API 만 사용

**Files:**
- Modify: `src/services/buy_criteria.py`
- Test: `tests/services/test_buy_criteria_policy_parity.py`

리뷰 #6 (private method 호출 금지) 핵심 반영.

- [ ] **Step 1: 실패 테스트 — buy_criteria 결과 = 현행 결과**

`tests/services/test_buy_criteria_policy_parity.py`:

```python
"""buy_criteria 가 ScoringPolicy public API 만 사용하여 현행과 동일 결과."""
from decimal import Decimal
from services.buy_criteria import BuyCriteria
from domain.recommendation.scoring_policy import ScoringPolicy


def test_calculate_uses_policy_public_api_only():
    p = ScoringPolicy.load_default()
    bc = BuyCriteria(policy=p)
    out = bc.calculate_composite_score(
        indicators={"golden_cross": True, "rsi": 50, "macd_buy_signal": True},
        ai_score=10.0,           # 이미 normalized 0~10
        sentiment_score=5.0,     # 이미 normalized 0~10
        has_ai=True,
        has_sentiment=True,
        has_tech=True,
    )
    # 현행: composite = 0.3*10 + 0.4*3.5 + 0.3*5 = 5.9
    assert out["composite_score"] == 5.9
    assert out["max_possible"] == 7.4    # 현행 buy_criteria 2.85 → 7.4 통일
    assert abs(out["confidence"] - 0.797) < 0.01
    assert out["missing_axes"] == []


def test_buy_criteria_does_not_call_private_policy_methods():
    """리뷰 #6: BuyCriteria 가 _calc_* 같은 private 호출 안 함."""
    import inspect
    from services import buy_criteria as bc_module
    source = inspect.getsource(bc_module)
    # private prefix 호출 금지
    assert "_calc_tech_score" not in source
    assert "_compose" not in source
    assert "_calc_sentiment_score" not in source
    assert "_calc_ai_score" not in source
```

- [ ] **Step 2: Test 실행 (실패)**

```bash
poetry run pytest tests/services/test_buy_criteria_policy_parity.py -v
```

- [ ] **Step 3: BuyCriteria 리팩토링**

`buy_criteria.py` 의 `calculate_composite_score`:

```python
from domain.recommendation.scoring_policy import ScoringPolicy
from decimal import Decimal


class BuyCriteria:
    def __init__(self, policy: Optional[ScoringPolicy] = None, ...):
        self._policy = policy or ScoringPolicy.load_default()
        # 기존 self.weight_ai / ScoreScale 의존 제거

    def calculate_composite_score(
        self,
        indicators: Dict[str, Any],
        ai_score: float,           # 이미 normalized (sync_service 와 동일 스케일)
        sentiment_score: float,
        has_ai: bool,
        has_sentiment: bool,
        has_tech: bool = True,
    ) -> Dict[str, Any]:
        """ScoringPolicy public API 위임. 분모 7.4 통일."""
        # tech_score / signal 수 는 policy public method
        tech_score = self._policy.tech_score_from_indicators(indicators) if has_tech else Decimal("0")
        tech_signals = self._policy.count_tech_signals(indicators) if has_tech else 0

        score = self._policy.compose_components(
            ai_score=Decimal(str(ai_score)) if has_ai else Decimal("0"),
            tech_score=tech_score,
            sentiment_score=Decimal(str(sentiment_score)) if has_sentiment else Decimal("0"),
            has_ai=has_ai,
            has_tech=has_tech,
            has_sentiment=has_sentiment,
            tech_signal_count=tech_signals,
        )

        return {
            "tech_score": float(score.tech_score),
            "tech_signals": tech_signals,
            "ai_score": float(score.ai_score),
            "sentiment_score": float(score.sentiment_score),
            "composite_score": round(float(score.composite_score), 4),
            "max_possible": round(float(score.composite_max), 4),
            "confidence": round(float(score.confidence), 4),
            "missing_axes": list(score.missing_axes),
        }
```

- [ ] **Step 4: ScoreScale 사용처 0 확인**

```bash
grep -rn "ScoreScale\|CRITERIA_MAX_" src/ tests/ 2>&1 | grep -v composite_grade.py
```

Expected: 0 hits. composite_grade.py 안의 ScoreScale 클래스 자체는 deprecated marker 만 추가 (실제 제거는 PR 2).

- [ ] **Step 5: Test 통과**

```bash
poetry run pytest tests/services/test_buy_criteria_policy_parity.py tests/services/ -v 2>&1 | tail -15
```

- [ ] **Step 6: Commit**

```bash
git add quant-jump-stock-data-engine/src/services/buy_criteria.py \
        quant-jump-stock-data-engine/tests/services/test_buy_criteria_policy_parity.py
git commit -m "refactor(buy_criteria): ScoringPolicy public API 위임 (max 7.4 통일)"
```

---

### Task 2.3: slack_notifier — 분모 동적 (`composite_max` 사용)

**Files:**
- Modify: `src/services/slack_notifier.py`
- Modify: `src/services/comprehensive_report.py`
- Test: `tests/services/test_slack_display_parity.py`

- [ ] **Step 1: 실패 테스트 — 분모가 spec 의 composite_max**

`tests/services/test_slack_display_parity.py`:

```python
"""Slack 분모가 ScoringPolicy.composite_max 에서. 현행 7.4 그대로 표시."""
from decimal import Decimal
from domain.recommendation.score import Score
from services.slack_notifier import _format_score_for_slack


def _score(composite: str, max_: str, confidence: str) -> Score:
    return Score(
        ai_score=Decimal("0"), tech_score=Decimal("0"), sentiment_score=Decimal("0"),
        composite_score=Decimal(composite), composite_max=Decimal(max_),
        confidence=Decimal(confidence), grade="A", recommendation_label="RECOMMEND",
    )


def test_format_uses_score_composite_max():
    s = _score("5.90", "7.40", "0.797")
    out = _format_score_for_slack(s)
    assert "5.90/7.40" in out  # 분모 7.40
    assert "80%" in out         # 0.797 → 80%
```

- [ ] **Step 2: Test 실패 확인**

- [ ] **Step 3: slack_notifier 변경**

`slack_notifier.py` 상단 import + helper:

```python
from decimal import Decimal
from domain.recommendation.score import Score
from domain.recommendation.scoring_policy import ScoringPolicy


def _format_score_for_slack(score: Score) -> str:
    """Score → Slack 표시 (분모를 score.composite_max 에서)."""
    return (
        f"종합점수: `{float(score.composite_score):.2f}/{float(score.composite_max):.2f}` "
        f"(달성도 {float(score.confidence):.0%})"
    )
```

line 591 근처 변경 (candidate 표시 블록):

```python
for i, rec in enumerate(candidates[:5], 1):
    score_obj: Optional[Score] = rec.get("score")
    if score_obj is not None:
        composite_str = _format_score_for_slack(score_obj)
        grade = score_obj.grade
        label_key = score_obj.recommendation_label
    else:
        # legacy fallback
        scores = rec.get("scores", {})
        composite = scores.get("composite_score", 0)
        confidence = scores.get("confidence", 0)
        policy = ScoringPolicy.load_default()
        composite_str = (
            f"종합점수: `{composite:.2f}/{float(policy.composite_max):.2f}` "
            f"(달성도 {confidence:.0%})"
        )
        grade = rec.get("grade", "D")
        label_key = grade if grade in {"STRONG","RECOMMEND","WATCH","NONE"} else "NONE"

    policy = ScoringPolicy.load_default()
    label_meta = policy.label_metadata(label_key)
    grade_emoji = label_meta["emoji"]
    grade_label = label_meta["label"]

    # ... signals 빌드 동일 ...

    blocks.append({
        "type": "section",
        "text": {
            "type": "mrkdwn",
            "text": (
                f"*{grade_emoji} {i}. {stock_name}* (`{ticker}`) — {grade_label}\n"
                f"• {composite_str} | 상승확률: `{rise_str}` | 감정: `{sent_str}`\n"
                f"• 기술신호: {signal_text}"
            )
        }
    })
```

`comprehensive_report.py` 의 rec 빌더가 `score` 필드를 가지도록 변경 (Task 2.1 sync_service 변경에 이미 포함).

- [ ] **Step 4: Test 통과**

```bash
poetry run pytest tests/services/test_slack_display_parity.py tests/services/ -v 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add quant-jump-stock-data-engine/src/services/slack_notifier.py \
        quant-jump-stock-data-engine/src/services/comprehensive_report.py \
        quant-jump-stock-data-engine/tests/services/test_slack_display_parity.py
git commit -m "refactor(slack): 종합점수 분모 ScoringPolicy.composite_max 에서 (현행 7.4 유지)"
```

---

## Phase 3 — 검증

### Task 3.1: composite_grade.ScoreScale — deprecated marker

**Files:** `src/domain/recommendation/composite_grade.py`

- [ ] **Step 1: ScoreScale 클래스에 deprecated marker 추가** (실제 제거는 PR 2)

```python
class ScoreScale:
    """DEPRECATED (PR 2 에서 제거). ScoringPolicy.axes 사용.

    호환을 위해 클래스는 유지하나 실제 호출처 0 (Task 2.2 에서 모두 제거됨).
    """
    # 기존 상수들은 그대로 유지. PR 2 에서 클래스 자체 삭제.
    SYNC_MAX_AI = Decimal("10")
    SYNC_MAX_TECH = Decimal("3.5")
    SYNC_MAX_SENTIMENT = Decimal("10")
    SYNC_MAX_COMPOSITE = Decimal("7.4")
    CRITERIA_MAX_AI = 3.5
    CRITERIA_MAX_TECH = 3.5
    CRITERIA_MAX_SENTIMENT = 1.0
    DEFAULT_WEIGHT_AI = Decimal("0.3")
    DEFAULT_WEIGHT_TECH = Decimal("0.4")
    DEFAULT_WEIGHT_SENTIMENT = Decimal("0.3")
```

- [ ] **Step 2: Commit**

```bash
git add quant-jump-stock-data-engine/src/domain/recommendation/composite_grade.py
git commit -m "refactor(scoring): ScoreScale deprecated marker (실제 제거는 PR 2)"
```

---

### Task 3.2: 통합 테스트 — sync_service `_merge_sources` mongo fixture

**Files:** `tests/application/recommendation/test_sync_integration.py`

리뷰 #5 핵심 반영.

- [ ] **Step 1: Mongo fixture 기반 통합 테스트**

```python
"""sync_service _merge_sources 통합 테스트 — mongo fixture 기반."""
import os
from datetime import datetime
from decimal import Decimal
from unittest.mock import MagicMock
import pytest
from application.recommendation.sync_service import RecommendationSyncService
from domain.recommendation.scoring_policy import ScoringPolicy


@pytest.fixture
def mongo_with_data():
    """In-memory mongo mock — 핵심 collection 시뮬레이션."""
    db = MagicMock()
    target = datetime(2026, 5, 15)
    # daily_stock_data
    db.daily_stock_data.find.return_value = []   # 별도 빌드
    # stock_recommendations
    rec_doc = {
        "ticker": "APP", "date": target, "stock_name": "앱플로빈",
        "technical_indicators": {
            "sma20": 470, "sma50": 449, "rsi": 54.86,
            "macd": 12.31, "signal": 10.21,
            "golden_cross": True, "macd_buy_signal": True,
        },
    }
    db.stock_recommendations.find.return_value = [rec_doc]
    # stock_analysis_results (현행 sync_service 가 fetch)
    ar_doc = {
        "ticker": "APP", "date": target,
        "predictions": {"rise_probability": 35.89, "predicted_future_price": 669.10},
        "metrics": {"accuracy": 84.19},
        "recommendation": "STRONG BUY",
    }
    db.stock_analysis_results.find.return_value = [ar_doc]
    # stock_predictions
    db.stock_predictions.find.return_value = [{
        "ticker": "APP", "date": target,
        "predicted_price": 669.10, "actual_price": 492.38,
    }]
    return db


def test_merge_sources_integration_preserves_current_behavior(mongo_with_data):
    """본 PR: APP 케이스에서 현행과 동일한 composite 산출 (행동 보존)."""
    policy = ScoringPolicy.load_default()
    svc = RecommendationSyncService(mongo_db=mongo_with_data, policy=policy)
    # sync_latest_recommendations 호출 또는 _merge_sources 직접 호출
    # (실제 시그니처에 맞게 조정. 본 plan 에선 통합 흐름 검증 목적)
    # 검증 포인트:
    #   - composite_score 가 현행과 일치 (이전 sync 결과 mock 으로 비교)
    #   - is_recommended 가 현행과 일치
    #   - score 객체에 missing_axes 정확히 채워짐
    # ...
    # 본 PR 에서는 sync_service.sync_latest_recommendations(["2026-05-15"]) 결과의
    # 첫 종목 composite_score 가 5.9 또는 그에 준하는 값인지 검증.
    pass  # 실제 작업 시 sync_service 시그니처에 맞게 작성
```

> **Note**: 실제 sync_service.sync_latest_recommendations 시그니처를 확인 후 통합 테스트 작성. mongo fixture 가 어렵다면 `mongomock` 라이브러리 도입 고려.

- [ ] **Step 2: Test 실행 + 통과**

```bash
poetry run pytest tests/application/recommendation/test_sync_integration.py -v
```

- [ ] **Step 3: Commit**

```bash
git add quant-jump-stock-data-engine/tests/application/recommendation/test_sync_integration.py
git commit -m "test(scoring): sync_service _merge_sources 통합 테스트 (mongo fixture)"
```

---

### Task 3.3: Golden CSV — 현행 동작 검증 매트릭스

**Files:**
- Create: `tests/data/golden_v1_0_0.csv`
- Create: `tests/domain/recommendation/test_golden.py`

- [ ] **Step 1: Golden CSV — 50 케이스 (현행 값 일치 검증 목적)**

```csv
case_id,scenario,ai_score,tech_score,sentiment_score,has_ai,has_tech,has_sentiment,tech_signals,expected_composite,expected_grade,expected_label
1,all_max,10.0,3.5,10.0,True,True,True,3,7.40,S,STRONG
2,all_zero,0,0,0,True,True,True,0,0.00,D,NONE
3,sync_app_realistic,10.0,3.5,0,True,True,True,3,4.40,B,STRONG
4,sentiment_missing,10.0,3.5,0,True,True,False,3,4.40,B,STRONG
5,ai_missing,0,3.5,5,False,True,True,3,2.90,C,WATCH
# ... 45 more 케이스 (boundary + missing 조합 + grade 경계)
```

- [ ] **Step 2: Golden runner 테스트**

```python
"""Golden CSV — ScoringPolicy.compose_components 결과 검증."""
import csv
from decimal import Decimal
from pathlib import Path
import pytest
from domain.recommendation.scoring_policy import ScoringPolicy


GOLDEN_PATH = Path(__file__).parents[2] / "data" / "golden_v1_0_0.csv"


def _b(v): return v.lower() == "true"


@pytest.fixture(scope="module")
def policy():
    return ScoringPolicy.load_default()


def _cases():
    with open(GOLDEN_PATH, newline="", encoding="utf-8") as f:
        return list(csv.DictReader(f))


@pytest.mark.parametrize("case", _cases(), ids=lambda c: c["case_id"])
def test_golden(case, policy):
    s = policy.compose_components(
        ai_score=Decimal(case["ai_score"]),
        tech_score=Decimal(case["tech_score"]),
        sentiment_score=Decimal(case["sentiment_score"]),
        has_ai=_b(case["has_ai"]),
        has_tech=_b(case["has_tech"]),
        has_sentiment=_b(case["has_sentiment"]),
        tech_signal_count=int(case["tech_signals"]),
    )
    exp_comp = Decimal(case["expected_composite"])
    assert abs(s.composite_score - exp_comp) <= Decimal("0.01"), f"{case['case_id']} composite {s.composite_score} != {exp_comp}"
    assert s.grade == case["expected_grade"], f"{case['case_id']} grade"
    assert s.recommendation_label == case["expected_label"], f"{case['case_id']} label"
```

- [ ] **Step 3: 통과 확인**

```bash
poetry run pytest tests/domain/recommendation/test_golden.py -v 2>&1 | tail -15
```

- [ ] **Step 4: Commit**

```bash
git add quant-jump-stock-data-engine/tests/data/golden_v1_0_0.csv \
        quant-jump-stock-data-engine/tests/domain/recommendation/test_golden.py
git commit -m "test(scoring): golden CSV 매트릭스 (50 케이스, 현행 동작 검증)"
```

---

### Task 3.4: Property test (Hypothesis)

**Files:** `tests/domain/recommendation/test_property.py`

- [ ] **Step 1: Property 작성**

```python
"""Property-based: monotonicity / range / weight invariant."""
from decimal import Decimal
from hypothesis import given, strategies as st, settings
from domain.recommendation.scoring_policy import ScoringPolicy


P = ScoringPolicy.load_default()


@given(
    ai=st.decimals(min_value=Decimal("0"), max_value=Decimal("10"), allow_nan=False),
    tech=st.decimals(min_value=Decimal("0"), max_value=Decimal("3.5"), allow_nan=False),
    sent=st.decimals(min_value=Decimal("0"), max_value=Decimal("10"), allow_nan=False),
)
@settings(max_examples=300, deadline=None)
def test_composite_within_range(ai, tech, sent):
    s = P.compose_components(
        ai_score=ai, tech_score=tech, sentiment_score=sent,
        has_ai=True, has_tech=True, has_sentiment=True,
        tech_signal_count=3,
    )
    assert Decimal("0") <= s.composite_score <= s.composite_max


@given(
    ai_lo=st.decimals(min_value=Decimal("0"), max_value=Decimal("9"), allow_nan=False),
    delta=st.decimals(min_value=Decimal("0"), max_value=Decimal("1"), allow_nan=False),
)
@settings(max_examples=200, deadline=None)
def test_ai_monotonic(ai_lo, delta):
    common = dict(tech_score=Decimal("3.5"), sentiment_score=Decimal("5"),
                  has_ai=True, has_tech=True, has_sentiment=True, tech_signal_count=3)
    s_lo = P.compose_components(ai_score=ai_lo, **common)
    s_hi = P.compose_components(ai_score=ai_lo + delta, **common)
    assert s_hi.composite_score >= s_lo.composite_score - Decimal("0.01")


def test_weight_sum_invariant():
    s = sum(Decimal(str(P.axes[k]["weight"])) for k in P.axes)
    assert abs(s - Decimal("1.0")) < Decimal("0.001")
```

- [ ] **Step 2: 통과**

```bash
poetry run pytest tests/domain/recommendation/test_property.py -v 2>&1 | tail -10
```

- [ ] **Step 3: Commit**

```bash
git add quant-jump-stock-data-engine/tests/domain/recommendation/test_property.py
git commit -m "test(scoring): Hypothesis property — monotonicity/range/invariant"
```

---

### Task 3.5: Prod regression 스크립트 — 의도된 변화 0건 검증

**Files:** `scripts/scoring_regression_prod.py`

리뷰 #11, #12 반영.

- [ ] **Step 1: Script 작성**

`scripts/scoring_regression_prod.py`:

```python
"""PR 1 prod regression — 의도된 변화 0건 검증.

⚠️ 운영 안전성:
  - Read-only MongoDB 계정으로만 실행.
  - .env.db.prod 직접 로드 금지. 환경변수 주입.
  - agent 자동 실행 금지. 사람이 PR 검토 직전 1회 실행.

실행 (운영자):
  MONGODB_URI_RO='mongodb+srv://readonly:...' \
  MONGODB_DB_NAME=stock_trading \
  SCORING_SPEC_PATH=/Users/.../scoring_spec.yaml \
  poetry run python scripts/scoring_regression_prod.py --days 7 --threshold 0.01

성공 기준:
  - delta > threshold 인 종목 0건 (의도된 변화 없음)
  - 0건 아니면 non-zero exit + JSON 저장 → PR 검토 시 첨부
"""
from __future__ import annotations
import argparse
import json
import os
import sys
from datetime import datetime, timedelta
from decimal import Decimal
from typing import Dict, List

from pymongo import MongoClient

from domain.recommendation.scoring_policy import ScoringPolicy


def main(days: int, threshold: float):
    uri = os.environ.get("MONGODB_URI_RO")
    db_name = os.environ.get("MONGODB_DB_NAME", "stock_trading")
    if not uri:
        print("❌ MONGODB_URI_RO env 필수 (read-only 계정)")
        return 2

    client = MongoClient(uri)
    db = client[db_name]
    policy = ScoringPolicy.load_default()

    cutoff = datetime.utcnow() - timedelta(days=days)
    # stock_recommendations 에서 N+3 회피 위해 ticker/date 모음
    rec_docs = list(db.stock_recommendations.find(
        {"date": {"$gte": cutoff}, "is_recommended": True},
        {"ticker": 1, "date": 1, "technical_indicators": 1, "composite_score": 1, "ai_score": 1, "sentiment_score": 1},
    ))
    if not rec_docs:
        print("⚠️ regression 대상 0건")
        return 0

    tickers = list({d["ticker"] for d in rec_docs})
    dates = list({d["date"] for d in rec_docs})

    # $in bulk fetch
    sp_map: Dict[tuple, Dict] = {(d["ticker"], d["date"]): d for d in db.stock_predictions.find(
        {"ticker": {"$in": tickers}, "date": {"$in": dates}})}
    ar_map: Dict[tuple, Dict] = {(d["ticker"], d["date"]): d for d in db.stock_analysis_results.find(
        {"ticker": {"$in": tickers}, "date": {"$in": dates}})}

    unexpected: List[Dict] = []
    for doc in rec_docs:
        key = (doc["ticker"], doc["date"])
        ar = ar_map.get(key, {})
        rise_prob = ar.get("predictions", {}).get("rise_probability")
        # 현행 sync_service: rise_probability 는 % 단위인 듯한 fetch (확인 필요).
        # 본 regression 은 동일 입력으로 신 산식 결과를 만들어 비교.
        # ScoringPolicy.score_from_raw_signals 호출
        new_score = policy.score_from_raw_signals(
            rise_pct=Decimal(str(rise_prob)) if rise_prob is not None else None,
            sentiment_raw=None,  # sentiment 별도 fetch 필요 — 본 회귀에서는 sync_service 와 동일 path 호출 권장
            tech_indicators=doc.get("technical_indicators"),
        )

        old_composite = doc.get("composite_score") or 0
        delta = abs(float(new_score.composite_score) - old_composite)
        if delta > threshold:
            unexpected.append({
                "ticker": doc["ticker"],
                "date": str(doc["date"]),
                "old_composite": old_composite,
                "new_composite": float(new_score.composite_score),
                "delta": delta,
            })

    print(f"총 검증 {len(rec_docs)} 건. 의도 외 변화: {len(unexpected)} 건")
    if unexpected:
        out_path = "regression_unexpected.json"
        with open(out_path, "w", encoding="utf-8") as f:
            json.dump(unexpected, f, indent=2, ensure_ascii=False)
        print(f"❌ 변화 발견. {out_path} 참고. PR 검토에 첨부.")
        return 1
    return 0


if __name__ == "__main__":
    parser = argparse.ArgumentParser()
    parser.add_argument("--days", type=int, default=7)
    parser.add_argument("--threshold", type=float, default=0.01)
    args = parser.parse_args()
    sys.exit(main(args.days, args.threshold))
```

- [ ] **Step 2: 운영자 실행 가이드 문서 — `docs/plans/점수모델_중앙화_운영가이드.md`**

(선택, PR 머지 직전 별도 commit. 본 plan 에서는 명시만)

- [ ] **Step 3: Commit**

```bash
git add quant-jump-stock-data-engine/scripts/scoring_regression_prod.py
git commit -m "test(scoring): prod 7일 regression — 의도된 변화 0건 검증 (read-only 계정)"
```

---

## Phase 4 — PR + 운영 검증

### Task 4.1: 전체 테스트 + 로컬 docker 검증

- [ ] **Step 1: 전체 unit + integration + golden + property 통과**

```bash
poetry run pytest tests/ -v 2>&1 | tail -20
```

- [ ] **Step 2: 로컬 Docker build + spec load 검증**

```bash
cd /Users/sfn1/Desktop/workSpace.nosync/quant-jump-stock
docker build -t qjs-data-engine:pr1 -f quant-jump-stock-backend/quant-jump-stock-data-engine/Dockerfile .
docker run --rm qjs-data-engine:pr1 cat /app/scoring_spec.yaml | head -5
docker run --rm qjs-data-engine:pr1 python -c "from domain.recommendation.scoring_policy import ScoringPolicy; print(ScoringPolicy.load_default().formula_version)"
```

Expected: spec 출력 + `1.0.0`.

- [ ] **Step 3: 로컬 docker-compose 통합 실행 (선택)**

```bash
docker compose up -d
# 추천 trigger 후 Slack 로컬 mock 로그 / DB 결과 확인 (현행과 동일 결과)
docker compose down
```

---

### Task 4.2: 운영자가 prod regression 실행 (사람 직접)

- [ ] **Step 1: 운영자 셸에서 read-only 계정 사용**

```bash
cd quant-jump-stock-data-engine
MONGODB_URI_RO='<read-only-uri>' \
MONGODB_DB_NAME=stock_trading \
SCORING_SPEC_PATH=/Users/.../scoring_spec.yaml \
  poetry run python scripts/scoring_regression_prod.py --days 7 --threshold 0.01
```

Expected: `의도 외 변화: 0 건`. 아니면 차이 분석 후 plan 수정.

- [ ] **Step 2: Regression 결과를 PR description 에 첨부**

---

### Task 4.3: PR 생성

- [ ] **Step 1: Push + PR**

```bash
git push -u origin refactor/scoring-policy-ssot-pr1
gh pr create --title "refactor(scoring): PR 1 — 점수 모델 SSoT 중앙화 (현행 행동 보존)" \
  --body "$(cat <<'EOF'
## 요약 — 5단계 PR 중 1단계

5/20 사용자 의문 (composite 2.40/7.4 + 강력추천 + 달성도 87% 모순) 후속.
산식이 6 파일 흩어져 있고 두 산식 시스템 (max 7.4 vs 2.85) 충돌. 본 PR 은 이를 `ScoringPolicy` + monorepo 루트 `scoring_spec.yaml` SSoT 로 통합. **산식 결과 100% 동일** (행동 보존). 행동 변경은 PR 2~5 에서 단계적 진행.

## PR 분리 전체 로드맵
- **PR 1 (본 PR)**: SSoT 중앙화 (현행 결과 동일)
- PR 2: PostgreSQL `prediction_results` + Kotlin Core 표시 계약
- PR 3: AI source disagreement + negative AI veto
- PR 4: sentiment missing 재분배 (ADR 후)
- PR 5: VIX 거시 gate

각 PR 머지 → regression 검증 → 다음 PR.

## 본 PR 변경

### 추가
- `/scoring_spec.yaml` (monorepo 루트, SSoT)
- `domain/recommendation/scoring_policy.py` (`ScoringPolicy` public API)
- `domain/recommendation/score.py` (`Score` immutable dataclass, tuple + veto_reasons 분리)
- `domain/recommendation/exceptions.py` (SpecValidationError / SpecNotFoundError)
- Golden / property / integration / prod regression 테스트

### 수정
- `sync_service`: 산식 4개 메서드 제거 → policy 위임. **결과 동일**
- `buy_criteria`: max 2.85 산식 제거 → policy 위임. **분모 7.4 통일**
- `slack_notifier`: 분모 `composite_max` 동적 사용 (현행 7.4 표시 유지)
- `Dockerfile` + `deploy-data-engine.yml`: monorepo 루트 build context + spec COPY
- `main.py`: startup spec load 검증 (fail-fast)
- `composite_grade.py`: `ScoreScale` deprecated marker (제거는 PR 2)

## 설계 원칙 12건 반영 (본 plan §0 매트릭스)
- **C1** Docker spec 포함 ✓ (Task 1.5)
- **C2** raw % vs normalized API 분리 ✓ (`normalize_rise_pct_to_score` / `ai_score_from_normalized`)
- **C3** sentiment 재분배 본 PR 미적용 ✓ (PR 4)
- **C4** PostgreSQL/Kotlin 계약 본 PR 미반영 ✓ (PR 2)
- **H5** mongo fixture 통합 테스트 ✓
- **H6** BuyCriteria public API 만 호출 ✓
- **H7** `Score` tuple + veto_reasons ✓
- **H8** VIX gate 본 PR 미적용 ✓ (PR 5)
- **M9** PyYAML / Hypothesis 의존성 ✓
- **M10** `ROUND_HALF_UP` quantize 명시 ✓
- **M11** prod regression read-only + 사람 실행 ✓
- **M12** $in bulk fetch ✓

## 검증
- [x] Unit tests
- [x] Golden CSV 50 케이스
- [x] Property (Hypothesis)
- [x] Integration (mongo fixture)
- [x] Docker build + spec load
- [x] Prod 7일 regression: 의도 외 변화 0건 (첨부)
- [ ] 머지 후 다음 cron (23:20 KST) 자연 실행 검증
- [ ] Slack 메시지 출력 글자 단위 동일 (회귀 없음)

## 후속 PR (별도)
PR 2~5 는 본 PR 머지 + regression 안정 후 진행.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 2: CI 통과 확인 + 머지**

```bash
gh pr checks <PR_NUM>
gh pr merge <PR_NUM> --merge --delete-branch
```

- [ ] **Step 3: 자동 deploy 모니터링**

```bash
gh run list --workflow=deploy-data-engine.yml --limit=1 --json status,conclusion
```

머지 후 다음 cron (23:20 KST) 자연 실행 → Slack 분석 채널 결과 확인:
- 메시지 형식 / 종목 순서 / 점수 / 라벨 모두 현행과 동일
- 다른 점 발견 시 즉시 분석 (의도 외 변화는 hot fix 검토)

---

## Self-Review

### 1. Spec coverage vs 상위 plan 문서
- ✅ Axis 1 (SSoT 중앙화): Phase 1-2 전체
- ✅ Axis 2 (의도된 결과 변화): **본 PR 미포함** — PR 3-4 로 명시 분리
- ✅ Axis 3 (VIX gate): **본 PR 미포함** — PR 5 로 분리
- ✅ 결함 1/2/8: Phase 1-2 에서 자동 해소
- ✅ 결함 3/6/7/9/4/5: **본 PR 미포함** — 각 별도 PR

### 2. 리뷰 12 findings 추적표 — §0 매트릭스에서 확인

### 3. Placeholder 0
- "TBD" 검색: 0 hit (단 Task 3.2 의 `pass  # 실제 작업 시...` 는 sync_service 시그니처 확정 후 작성 — 실행 시 인지)

### 4. 이름 일관성
- `ScoringPolicy.load_default()` / `score_from_raw_signals()` / `compose_components()` / `ai_score_from_normalized()` / `tech_score_from_indicators()` / `sentiment_score_from_raw()` / `label_metadata()` — 일관 명명

### 5. 알려진 단순화 (의도적)
- Task 3.2 integration test 의 sync_service.sync_latest_recommendations 호출은 시그니처 확정 후 작성 (mongo mock vs mongomock 선택)
- Golden CSV 50 케이스 — 본 PR 은 *현행 보존* 검증 목적이라 매트릭스 작아도 충분. PR 3-5 머지 시 케이스 확장.

---

## 변경 이력

| 날짜 | 작성자 | 변경 |
|------|--------|------|
| 2026-05-20 | Claude (Opus 4.7) | PR 1 (현행 행동 보존 중앙화) plan 확정. 설계 원칙 12건 반영. |
