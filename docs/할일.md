# 백엔드 할일

## 코드 리뷰 후속 작업 (2026-02-20)

### 🔴 HIGH - 즉시 처리

- [ ] **[Data Engine] Pub/Sub 에러 처리 수정**
  - 파일: `quant-jump-stock-data-engine/src/adapter/input/pubsub/subscriber.py:116-130`
  - 문제: 모든 예외를 `message.ack()` 처리 → 재시도 불가, 메시지 영구 소실
  - 수정: `NonRetryableError`만 `ack()`, 나머지는 `nack()` 또는 예외 rethrow
  - 연관: `push_handler.py:119-127` 동일 패턴 수정 필요

- [ ] **[Data Engine] async 핸들러 타입 불일치**
  - 파일: `quant-jump-stock-data-engine/src/adapter/input/pubsub/handlers.py`
  - 문제: `MessageHandler.handle()` sync 정의 → `TechnicalAnalysisHandler.handle()` async override → 런타임 오류 가능
  - 수정: base class를 `async def`로 통일하거나 async 전용 프로토콜 분리

---

### 🟡 MEDIUM - 이번 스프린트

- [ ] **[Core API] JWT 검증 오류 무시**
  - 파일: `quant-jump-stock-core/.../config/JwtAuthenticationFilter.kt:51`
  - 문제: 만료/변조 토큰을 `catch(e: Exception)` 으로 경고 로그만 남기고 요청 통과
  - 수정: `ExpiredJwtException` → 401 응답, `SignatureException` → 401 + 보안 로그

- [ ] **[Core API] TOCTOU Race Condition**
  - 파일: `quant-jump-stock-core/.../adapter/input/rest/backtest/BacktestController.kt:99`
  - 문제: 백테스트 쿼터 체크 후 실제 생성 사이에 동시 요청이 끼어들 수 있음
  - 수정: `@Transactional` + `SELECT FOR UPDATE` 또는 DB 유니크 제약으로 처리

- [ ] **[Core API] 예외 처리 과도하게 광범위**
  - 파일: `quant-jump-stock-core/.../adapter/input/rest/prediction/PredictionController.kt:35`
  - 문제: 모든 예외를 `catch(e: Exception)` 으로 500 응답 → 클라이언트 혼동
  - 수정: 도메인 예외별 HTTP 상태코드 분리 (404, 503, 500 등)

- [ ] **[Data Engine] MongoDB 커넥션 미해제**
  - 파일: `quant-jump-stock-data-engine/src/application/backtest/data_loader_mongo.py:82`
  - 문제: `MongoClient` 캐싱 후 명시적 close 없음
  - 수정: `__del__` 또는 context manager 추가

- [ ] **[Data Engine] PostgreSQL context manager 코드 중복**
  - 파일: `stock_repository.py`, `backtest_repository.py`, `collector_state_repository.py`
  - 문제: 동일한 13줄 `_get_connection()` 패턴이 3곳에 복붙
  - 수정: `core/postgres_context.py` 공통 유틸로 추출

- [ ] **[Data Engine] Decimal → float 정밀도 손실**
  - 파일: `quant-jump-stock-data-engine/src/adapter/output/postgresql/backtest_repository.py:132`
  - 문제: 금융 데이터(수익률, 자산 가치)를 `float()` 으로 변환 시 정밀도 손실
  - 수정: `Decimal` 유지 또는 JSON 직렬화 시 커스텀 인코더 사용

---

### 🟢 LOW - 다음 스프린트

- [ ] **[Data Engine] settings.py 프로퍼티 중복**
  - 파일: `quant-jump-stock-data-engine/src/config/settings.py:134-233`
  - 문제: 20개 이상의 프로퍼티가 단순히 하위 설정을 재노출하는 중복 구조
  - 수정: Pydantic `alias` 또는 `model_validator` 활용

- [ ] **[Data Engine] 로그에 이모지 사용**
  - 파일: `quant-jump-stock-data-engine/src/adapter/input/pubsub/handlers.py`
  - 문제: `✅`, `❌` 이모지가 로그 수집 도구(CloudWatch, Loki 등)에서 파싱 깨질 수 있음
  - 수정: 이모지 제거, 구조화 로깅 (`extra={"status": "success"}`) 으로 교체

- [ ] **[Core API] 하드코딩된 에러 메시지 상수화**
  - 예: `"인증이 필요합니다"`, `"접근 권한이 없습니다"` 등 반복되는 문자열
  - 수정: `ErrorMessages` object 또는 enum으로 중앙화

---

## 완료된 작업

- [x] MongoDB 인덱스 추가 (daily_stock_data, stock_recommendations, sentiment_analysis)
- [x] 미사용 컬렉션 및 dead code 제거 (stock_prices, technical_indicators, MongoMarketDataRepository)
- [x] **[Core API] 인증 우회 주석 해제** — `UserBalanceController.validateUserAccess()`
- [x] **[Core API] @Transactional 누락 수정** — `AuthService.signup()`
- [x] **[Core API] OAuth 계정 비번 로그인 오류 메시지 분리** — `AuthService.login()`
- [x] **[Core API] !! 연산자 제거** — 5개 파일 10곳
- [x] **[Core API] N+1 쿼리 제거** — `UserPortfolioService.getUserPortfolios()` GROUP BY 배치 쿼리로 교체
